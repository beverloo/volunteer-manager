name: anime
tables:
- name: environments
  type: BASE TABLE
  columns:
  - name: environment_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: environment_colour_dark_mode
    type: varchar(16)
    nullable: false
  - name: environment_colour_light_mode
    type: varchar(16)
    nullable: false
  - name: environment_description
    type: text
    nullable: false
  - name: environment_domain
    type: varchar(64)
    nullable: false
  - name: environment_purpose
    type: enum('LandingPage','Placeholder')
    nullable: false
    default: "'LandingPage'"
  - name: environment_title
    type: text
    nullable: false
  - name: environment_deleted
    type: datetime
    nullable: true
    default: "NULL"
  indexes:
  - name: PRIMARY
    def: PRIMARY KEY (environment_id) USING BTREE
    table: environments
    columns:
    - environment_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (environment_id)
    table: environments
    columns:
    - environment_id
  def: |-
    CREATE TABLE `environments` (
      `environment_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `environment_colour_dark_mode` varchar(16) NOT NULL,
      `environment_colour_light_mode` varchar(16) NOT NULL,
      `environment_description` text NOT NULL,
      `environment_domain` varchar(64) NOT NULL,
      `environment_purpose` enum('LandingPage','Placeholder') NOT NULL DEFAULT 'LandingPage',
      `environment_title` text NOT NULL,
      `environment_deleted` datetime DEFAULT NULL,
      PRIMARY KEY (`environment_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: events_teams
  type: BASE TABLE
  columns:
  - name: event_id
    type: int(4) unsigned
    nullable: false
  - name: team_id
    type: int(4) unsigned
    nullable: false
  - name: team_target_size
    type: int(4) unsigned
    nullable: false
  - name: team_maximum_size
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: enable_team
    type: tinyint(1)
    nullable: false
    default: "0"
  - name: enable_applications_start
    type: datetime
    nullable: true
    default: "NULL"
  - name: enable_applications_end
    type: datetime
    nullable: true
    default: "NULL"
  - name: enable_registration_start
    type: datetime
    nullable: true
    default: "NULL"
  - name: enable_registration_end
    type: datetime
    nullable: true
    default: "NULL"
  - name: enable_schedule_start
    type: datetime
    nullable: true
    default: "NULL"
  - name: enable_schedule_end
    type: datetime
    nullable: true
    default: "NULL"
  - name: whatsapp_link
    type: text
    nullable: true
    default: "NULL"
  indexes:
  - name: event_id
    def: KEY event_id (event_id) USING BTREE
    table: events_teams
    columns:
    - event_id
  - name: team_id
    def: KEY team_id (team_id) USING BTREE
    table: events_teams
    columns:
    - team_id
  - name: event_id_2
    def: UNIQUE KEY event_id_2 (event_id, team_id) USING BTREE
    table: events_teams
    columns:
    - event_id
    - team_id
  constraints:
  - name: events_teams_event_id
    type: FOREIGN KEY
    def: FOREIGN KEY (event_id) REFERENCES events (event_id)
    table: events_teams
    referencedTable: events
    columns:
    - event_id
    referencedColumns:
    - event_id
  - name: events_teams_team_id
    type: FOREIGN KEY
    def: FOREIGN KEY (team_id) REFERENCES teams (team_id)
    table: events_teams
    referencedTable: teams
    columns:
    - team_id
    referencedColumns:
    - team_id
  - name: event_id_2
    type: UNIQUE
    def: UNIQUE KEY event_id_2 (event_id, team_id)
    table: events_teams
    columns:
    - event_id
    - team_id
  def: |-
    CREATE TABLE `events_teams` (
      `event_id` int(4) unsigned NOT NULL,
      `team_id` int(4) unsigned NOT NULL,
      `team_target_size` int(4) unsigned NOT NULL,
      `team_maximum_size` int(8) unsigned DEFAULT NULL,
      `enable_team` tinyint(1) NOT NULL DEFAULT 0,
      `enable_applications_start` datetime DEFAULT NULL,
      `enable_applications_end` datetime DEFAULT NULL,
      `enable_registration_start` datetime DEFAULT NULL,
      `enable_registration_end` datetime DEFAULT NULL,
      `enable_schedule_start` datetime DEFAULT NULL,
      `enable_schedule_end` datetime DEFAULT NULL,
      `whatsapp_link` text DEFAULT NULL,
      UNIQUE KEY `event_id_2` (`event_id`,`team_id`),
      KEY `event_id` (`event_id`),
      KEY `team_id` (`team_id`),
      CONSTRAINT `events_teams_event_id` FOREIGN KEY (`event_id`) REFERENCES `events` (`event_id`) ON DELETE CASCADE,
      CONSTRAINT `events_teams_team_id` FOREIGN KEY (`team_id`) REFERENCES `teams` (`team_id`) ON DELETE CASCADE
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: hotels
  type: BASE TABLE
  columns:
  - name: hotel_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: event_id
    type: int(8) unsigned
    nullable: false
  - name: hotel_name
    type: varchar(64)
    nullable: false
    default: "''"
  - name: hotel_description
    type: text
    nullable: false
    default: "''"
  - name: hotel_room_name
    type: varchar(64)
    nullable: false
    default: "''"
  - name: hotel_room_people
    type: int(8)
    nullable: false
    default: "1"
  - name: hotel_room_price
    type: int(8)
    nullable: false
    default: "0"
  - name: hotel_room_visible
    type: tinyint(1)
    nullable: false
    default: "1"
  indexes:
  - name: event_id
    def: KEY event_id (event_id) USING BTREE
    table: hotels
    columns:
    - event_id
  - name: PRIMARY
    def: PRIMARY KEY (hotel_id) USING BTREE
    table: hotels
    columns:
    - hotel_id
  constraints:
  - name: hotels_event_id
    type: FOREIGN KEY
    def: FOREIGN KEY (event_id) REFERENCES events (event_id)
    table: hotels
    referencedTable: events
    columns:
    - event_id
    referencedColumns:
    - event_id
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (hotel_id)
    table: hotels
    columns:
    - hotel_id
  def: |-
    CREATE TABLE `hotels` (
      `hotel_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `event_id` int(8) unsigned NOT NULL,
      `hotel_name` varchar(64) NOT NULL DEFAULT '',
      `hotel_description` text NOT NULL DEFAULT '',
      `hotel_room_name` varchar(64) NOT NULL DEFAULT '',
      `hotel_room_people` int(8) NOT NULL DEFAULT 1,
      `hotel_room_price` int(8) NOT NULL DEFAULT 0,
      `hotel_room_visible` tinyint(1) NOT NULL DEFAULT 1,
      PRIMARY KEY (`hotel_id`),
      KEY `event_id` (`event_id`),
      CONSTRAINT `hotels_event_id` FOREIGN KEY (`event_id`) REFERENCES `events` (`event_id`) ON DELETE CASCADE
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: roles
  type: BASE TABLE
  columns:
  - name: role_id
    type: int(4) unsigned
    nullable: false
    extraDef: auto_increment
  - name: role_name
    type: varchar(32)
    nullable: false
  - name: role_badge
    type: enum('Staff','Senior','Host')
    nullable: true
    default: "NULL"
  - name: role_order
    type: int(4)
    nullable: false
    default: "0"
  - name: role_availability_event_limit
    type: int(8) unsigned
    nullable: false
  - name: role_permission_grant
    type: text
    nullable: true
    default: "NULL"
  - name: role_flag_default_restricted
    type: tinyint(1) unsigned
    nullable: false
    default: "0"
  - name: role_admin_access
    type: tinyint(1)
    nullable: false
    default: "0"
  - name: role_hotel_eligible
    type: tinyint(1)
    nullable: false
    default: "0"
  - name: role_schedule_collapse
    type: tinyint(1) unsigned
    nullable: false
    default: "0"
  - name: role_training_eligible
    type: tinyint(1)
    nullable: false
    default: "0"
  indexes:
  - name: PRIMARY
    def: PRIMARY KEY (role_id) USING BTREE
    table: roles
    columns:
    - role_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (role_id)
    table: roles
    columns:
    - role_id
  def: |-
    CREATE TABLE `roles` (
      `role_id` int(4) unsigned NOT NULL AUTO_INCREMENT,
      `role_name` varchar(32) NOT NULL,
      `role_badge` enum('Staff','Senior','Host') DEFAULT NULL,
      `role_order` int(4) NOT NULL DEFAULT 0,
      `role_availability_event_limit` int(8) unsigned NOT NULL,
      `role_permission_grant` text DEFAULT NULL,
      `role_flag_default_restricted` tinyint(1) unsigned NOT NULL DEFAULT 0,
      `role_admin_access` tinyint(1) NOT NULL DEFAULT 0,
      `role_hotel_eligible` tinyint(1) NOT NULL DEFAULT 0,
      `role_schedule_collapse` tinyint(1) unsigned NOT NULL DEFAULT 0,
      `role_training_eligible` tinyint(1) NOT NULL DEFAULT 0,
      PRIMARY KEY (`role_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: exports
  type: BASE TABLE
  columns:
  - name: export_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: export_slug
    type: varchar(16)
    nullable: false
  - name: export_event_id
    type: int(8) unsigned
    nullable: false
  - name: export_type
    type: enum('Credits','Discord','Refunds','Trainings','Volunteers','WhatsApp')
    nullable: false
  - name: export_justification
    type: text
    nullable: false
  - name: export_created_date
    type: datetime
    nullable: false
  - name: export_created_user_id
    type: int(8) unsigned
    nullable: false
  - name: export_expiration_date
    type: datetime
    nullable: false
  - name: export_expiration_views
    type: int(8) unsigned
    nullable: false
  - name: export_enabled
    type: tinyint(1) unsigned
    nullable: false
    default: "1"
  indexes:
  - name: export_slug
    def: KEY export_slug (export_slug) USING BTREE
    table: exports
    columns:
    - export_slug
  - name: PRIMARY
    def: PRIMARY KEY (export_id) USING BTREE
    table: exports
    columns:
    - export_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (export_id)
    table: exports
    columns:
    - export_id
  def: |-
    CREATE TABLE `exports` (
      `export_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `export_slug` varchar(16) NOT NULL,
      `export_event_id` int(8) unsigned NOT NULL,
      `export_type` enum('Credits','Discord','Refunds','Trainings','Volunteers','WhatsApp') NOT NULL,
      `export_justification` text NOT NULL,
      `export_created_date` datetime NOT NULL,
      `export_created_user_id` int(8) unsigned NOT NULL,
      `export_expiration_date` datetime NOT NULL,
      `export_expiration_views` int(8) unsigned NOT NULL,
      `export_enabled` tinyint(1) unsigned NOT NULL DEFAULT 1,
      PRIMARY KEY (`export_id`),
      KEY `export_slug` (`export_slug`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: twilio_webhook_calls
  type: BASE TABLE
  columns:
  - name: webhook_call_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: webhook_call_date
    type: datetime
    nullable: false
  - name: webhook_call_endpoint
    type: enum('Inbound','Outbound')
    nullable: false
  - name: webhook_request_source
    type: varchar(64)
    nullable: true
    default: "NULL"
  - name: webhook_request_method
    type: varchar(32)
    nullable: false
  - name: webhook_request_url
    type: text
    nullable: false
  - name: webhook_request_headers
    type: text
    nullable: false
  - name: webhook_request_body
    type: text
    nullable: false
  - name: webhook_request_signature
    type: varchar(64)
    nullable: true
    default: "NULL"
  - name: webhook_request_authenticated
    type: tinyint(1) unsigned
    nullable: false
    default: "0"
  - name: webhook_message_sid
    type: varchar(64)
    nullable: true
    default: "NULL"
  - name: webhook_message_original_sid
    type: varchar(64)
    nullable: true
    default: "NULL"
  - name: webhook_error_name
    type: varchar(32)
    nullable: true
    default: "NULL"
  - name: webhook_error_cause
    type: text
    nullable: true
    default: "NULL"
  - name: webhook_error_message
    type: text
    nullable: true
    default: "NULL"
  - name: webhook_error_stack
    type: text
    nullable: true
    default: "NULL"
  indexes:
  - name: webhook_message_original_sid
    def: KEY webhook_message_original_sid (webhook_message_original_sid) USING BTREE
    table: twilio_webhook_calls
    columns:
    - webhook_message_original_sid
  - name: webhook_message_sid
    def: KEY webhook_message_sid (webhook_message_sid) USING BTREE
    table: twilio_webhook_calls
    columns:
    - webhook_message_sid
  - name: PRIMARY
    def: PRIMARY KEY (webhook_call_id) USING BTREE
    table: twilio_webhook_calls
    columns:
    - webhook_call_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (webhook_call_id)
    table: twilio_webhook_calls
    columns:
    - webhook_call_id
  def: |-
    CREATE TABLE `twilio_webhook_calls` (
      `webhook_call_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `webhook_call_date` datetime NOT NULL,
      `webhook_call_endpoint` enum('Inbound','Outbound') NOT NULL,
      `webhook_request_source` varchar(64) DEFAULT NULL,
      `webhook_request_method` varchar(32) NOT NULL,
      `webhook_request_url` text NOT NULL,
      `webhook_request_headers` text NOT NULL,
      `webhook_request_body` text NOT NULL,
      `webhook_request_signature` varchar(64) DEFAULT NULL,
      `webhook_request_authenticated` tinyint(1) unsigned NOT NULL DEFAULT 0,
      `webhook_message_sid` varchar(64) DEFAULT NULL,
      `webhook_message_original_sid` varchar(64) DEFAULT NULL,
      `webhook_error_name` varchar(32) DEFAULT NULL,
      `webhook_error_cause` text DEFAULT NULL,
      `webhook_error_message` text DEFAULT NULL,
      `webhook_error_stack` text DEFAULT NULL,
      PRIMARY KEY (`webhook_call_id`),
      KEY `webhook_message_sid` (`webhook_message_sid`),
      KEY `webhook_message_original_sid` (`webhook_message_original_sid`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: events_sales
  type: BASE TABLE
  columns:
  - name: event_id
    type: int(8) unsigned
    nullable: false
  - name: event_sale_id
    type: int(8) unsigned
    nullable: false
  - name: event_sale_date
    type: date
    nullable: false
  - name: event_sale_count
    type: int(8)
    nullable: false
  - name: event_sale_updated
    type: datetime
    nullable: false
  indexes:
  - name: event_id
    def: UNIQUE KEY event_id (event_id, event_sale_id, event_sale_date) USING BTREE
    table: events_sales
    columns:
    - event_id
    - event_sale_id
    - event_sale_date
  constraints:
  - name: event_id
    type: UNIQUE
    def: UNIQUE KEY event_id (event_id, event_sale_id, event_sale_date)
    table: events_sales
    columns:
    - event_id
    - event_sale_id
    - event_sale_date
  def: |-
    CREATE TABLE `events_sales` (
      `event_id` int(8) unsigned NOT NULL,
      `event_sale_id` int(8) unsigned NOT NULL,
      `event_sale_date` date NOT NULL,
      `event_sale_count` int(8) NOT NULL,
      `event_sale_updated` datetime NOT NULL,
      UNIQUE KEY `event_id` (`event_id`,`event_sale_id`,`event_sale_date`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: displays
  type: BASE TABLE
  columns:
  - name: display_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: display_identifier
    type: varchar(4)
    nullable: false
  - name: display_label
    type: varchar(64)
    nullable: true
    default: "NULL"
  - name: display_event_id
    type: int(8)
    nullable: true
    default: "NULL"
  - name: display_location_id
    type: int(8)
    nullable: true
    default: "NULL"
  - name: display_color
    type: varchar(8)
    nullable: true
    default: "NULL"
  - name: display_help_request_status
    type: enum('Pending','Acknowledged')
    nullable: true
    default: "NULL"
  - name: display_check_in
    type: datetime
    nullable: false
  - name: display_check_in_ip
    type: varchar(64)
    nullable: false
  - name: display_locked
    type: tinyint(1)
    nullable: false
  - name: display_deleted
    type: datetime
    nullable: true
    default: "NULL"
  indexes:
  - name: PRIMARY
    def: PRIMARY KEY (display_id) USING BTREE
    table: displays
    columns:
    - display_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (display_id)
    table: displays
    columns:
    - display_id
  def: |-
    CREATE TABLE `displays` (
      `display_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `display_identifier` varchar(4) NOT NULL,
      `display_label` varchar(64) DEFAULT NULL,
      `display_event_id` int(8) DEFAULT NULL,
      `display_location_id` int(8) DEFAULT NULL,
      `display_color` varchar(8) DEFAULT NULL,
      `display_help_request_status` enum('Pending','Acknowledged') DEFAULT NULL,
      `display_check_in` datetime NOT NULL,
      `display_check_in_ip` varchar(64) NOT NULL,
      `display_locked` tinyint(1) NOT NULL,
      `display_deleted` datetime DEFAULT NULL,
      PRIMARY KEY (`display_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: refunds
  type: BASE TABLE
  columns:
  - name: user_id
    type: int(8) unsigned
    nullable: false
  - name: event_id
    type: int(8) unsigned
    nullable: false
  - name: refund_ticket_number
    type: text
    nullable: true
    default: "NULL"
  - name: refund_account_iban
    type: text
    nullable: false
  - name: refund_account_name
    type: text
    nullable: false
  - name: refund_requested
    type: datetime
    nullable: false
    default: current_timestamp()
  - name: refund_confirmed
    type: datetime
    nullable: true
    default: "NULL"
  indexes:
  - name: PRIMARY
    def: PRIMARY KEY (user_id, event_id) USING BTREE
    table: refunds
    columns:
    - user_id
    - event_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (user_id, event_id)
    table: refunds
    columns:
    - user_id
    - event_id
  def: |-
    CREATE TABLE `refunds` (
      `user_id` int(8) unsigned NOT NULL,
      `event_id` int(8) unsigned NOT NULL,
      `refund_ticket_number` text DEFAULT NULL,
      `refund_account_iban` text NOT NULL,
      `refund_account_name` text NOT NULL,
      `refund_requested` datetime NOT NULL DEFAULT current_timestamp(),
      `refund_confirmed` datetime DEFAULT NULL,
      PRIMARY KEY (`user_id`,`event_id`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: settings
  type: BASE TABLE
  columns:
  - name: setting_name
    type: varchar(64)
    nullable: false
  - name: setting_value
    type: text
    nullable: false
  indexes:
  - name: setting_name
    def: UNIQUE KEY setting_name (setting_name) USING BTREE
    table: settings
    columns:
    - setting_name
  constraints:
  - name: setting_name
    type: UNIQUE
    def: UNIQUE KEY setting_name (setting_name)
    table: settings
    columns:
    - setting_name
  def: |-
    CREATE TABLE `settings` (
      `setting_name` varchar(64) NOT NULL,
      `setting_value` text NOT NULL,
      UNIQUE KEY `setting_name` (`setting_name`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: storage
  type: BASE TABLE
  columns:
  - name: file_id
    type: int(4) unsigned
    nullable: false
    extraDef: auto_increment
  - name: file_hash
    type: varchar(16)
    nullable: false
  - name: file_type
    type: enum('Avatar','EventIdentity')
    nullable: false
  - name: file_mime_type
    type: varchar(64)
    nullable: false
  - name: file_date
    type: datetime
    nullable: false
  - name: file_data
    type: mediumblob
    nullable: false
  - name: user_id
    type: int(4) unsigned
    nullable: true
    default: "NULL"
  indexes:
  - name: file_hash
    def: KEY file_hash (file_hash) USING BTREE
    table: storage
    columns:
    - file_hash
  - name: storage_user_id
    def: KEY storage_user_id (user_id) USING BTREE
    table: storage
    columns:
    - user_id
  - name: PRIMARY
    def: PRIMARY KEY (file_id) USING BTREE
    table: storage
    columns:
    - file_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (file_id)
    table: storage
    columns:
    - file_id
  - name: storage_user_id
    type: FOREIGN KEY
    def: FOREIGN KEY (user_id) REFERENCES users (user_id)
    table: storage
    referencedTable: users
    columns:
    - user_id
    referencedColumns:
    - user_id
  def: |-
    CREATE TABLE `storage` (
      `file_id` int(4) unsigned NOT NULL AUTO_INCREMENT,
      `file_hash` varchar(16) NOT NULL,
      `file_type` enum('Avatar','EventIdentity') NOT NULL,
      `file_mime_type` varchar(64) NOT NULL,
      `file_date` datetime NOT NULL,
      `file_data` mediumblob NOT NULL,
      `user_id` int(4) unsigned DEFAULT NULL,
      PRIMARY KEY (`file_id`),
      KEY `file_hash` (`file_hash`),
      KEY `storage_user_id` (`user_id`),
      CONSTRAINT `storage_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`user_id`) ON DELETE CASCADE
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: events_deadlines
  type: BASE TABLE
  columns:
  - name: deadline_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: event_id
    type: int(8) unsigned
    nullable: false
  - name: deadline_date
    type: date
    nullable: false
  - name: deadline_owner_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: deadline_title
    type: text
    nullable: false
  - name: deadline_description
    type: text
    nullable: false
  - name: deadline_completed
    type: datetime
    nullable: true
    default: "NULL"
  - name: deadline_deleted
    type: datetime
    nullable: true
    default: "NULL"
  indexes:
  - name: event_id
    def: KEY event_id (event_id) USING BTREE
    table: events_deadlines
    columns:
    - event_id
  - name: PRIMARY
    def: PRIMARY KEY (deadline_id) USING BTREE
    table: events_deadlines
    columns:
    - deadline_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (deadline_id)
    table: events_deadlines
    columns:
    - deadline_id
  def: |-
    CREATE TABLE `events_deadlines` (
      `deadline_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `event_id` int(8) unsigned NOT NULL,
      `deadline_date` date NOT NULL,
      `deadline_owner_id` int(8) unsigned DEFAULT NULL,
      `deadline_title` text NOT NULL,
      `deadline_description` text NOT NULL,
      `deadline_completed` datetime DEFAULT NULL,
      `deadline_deleted` datetime DEFAULT NULL,
      PRIMARY KEY (`deadline_id`),
      KEY `event_id` (`event_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: teams_roles
  type: BASE TABLE
  columns:
  - name: team_id
    type: int(4) unsigned
    nullable: false
  - name: role_id
    type: int(4) unsigned
    nullable: false
  - name: role_default
    type: tinyint(1)
    nullable: false
    default: "0"
  indexes:
  - name: teams_roles_role_id
    def: KEY teams_roles_role_id (role_id) USING BTREE
    table: teams_roles
    columns:
    - role_id
  - name: team_id
    def: KEY team_id (team_id) USING BTREE
    table: teams_roles
    columns:
    - team_id
  constraints:
  - name: teams_roles_role_id
    type: FOREIGN KEY
    def: FOREIGN KEY (role_id) REFERENCES roles (role_id)
    table: teams_roles
    referencedTable: roles
    columns:
    - role_id
    referencedColumns:
    - role_id
  - name: teams_roles_team_id
    type: FOREIGN KEY
    def: FOREIGN KEY (team_id) REFERENCES teams (team_id)
    table: teams_roles
    referencedTable: teams
    columns:
    - team_id
    referencedColumns:
    - team_id
  def: |-
    CREATE TABLE `teams_roles` (
      `team_id` int(4) unsigned NOT NULL,
      `role_id` int(4) unsigned NOT NULL,
      `role_default` tinyint(1) NOT NULL DEFAULT 0,
      KEY `team_id` (`team_id`),
      KEY `teams_roles_role_id` (`role_id`),
      CONSTRAINT `teams_roles_role_id` FOREIGN KEY (`role_id`) REFERENCES `roles` (`role_id`) ON DELETE CASCADE,
      CONSTRAINT `teams_roles_team_id` FOREIGN KEY (`team_id`) REFERENCES `teams` (`team_id`) ON DELETE CASCADE
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: trainings
  type: BASE TABLE
  columns:
  - name: training_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: event_id
    type: int(8) unsigned
    nullable: false
  - name: training_start
    type: datetime
    nullable: false
    default: current_timestamp()
  - name: training_end
    type: datetime
    nullable: false
    default: current_timestamp()
  - name: training_address
    type: text
    nullable: true
    default: "NULL"
  - name: training_capacity
    type: int(4)
    nullable: false
    default: "10"
  - name: training_visible
    type: tinyint(1)
    nullable: false
    default: "1"
  indexes:
  - name: event_id
    def: KEY event_id (event_id) USING BTREE
    table: trainings
    columns:
    - event_id
  - name: PRIMARY
    def: PRIMARY KEY (training_id) USING BTREE
    table: trainings
    columns:
    - training_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (training_id)
    table: trainings
    columns:
    - training_id
  - name: trainings_event_id
    type: FOREIGN KEY
    def: FOREIGN KEY (event_id) REFERENCES events (event_id)
    table: trainings
    referencedTable: events
    columns:
    - event_id
    referencedColumns:
    - event_id
  def: |-
    CREATE TABLE `trainings` (
      `training_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `event_id` int(8) unsigned NOT NULL,
      `training_start` datetime NOT NULL DEFAULT current_timestamp(),
      `training_end` datetime NOT NULL DEFAULT current_timestamp(),
      `training_address` text DEFAULT NULL,
      `training_capacity` int(4) NOT NULL DEFAULT 10,
      `training_visible` tinyint(1) NOT NULL DEFAULT 1,
      PRIMARY KEY (`training_id`),
      KEY `event_id` (`event_id`),
      CONSTRAINT `trainings_event_id` FOREIGN KEY (`event_id`) REFERENCES `events` (`event_id`) ON DELETE CASCADE
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: trainings_extra
  type: BASE TABLE
  columns:
  - name: training_extra_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: event_id
    type: int(8) unsigned
    nullable: false
  - name: training_extra_name
    type: varchar(128)
    nullable: false
    default: "''"
  - name: training_extra_email
    type: varchar(128)
    nullable: false
    default: "''"
  - name: training_extra_birthdate
    type: date
    nullable: true
    default: "NULL"
  - name: training_extra_visible
    type: tinyint(1)
    nullable: false
    default: "1"
  indexes:
  - name: event_id
    def: KEY event_id (event_id) USING BTREE
    table: trainings_extra
    columns:
    - event_id
  - name: PRIMARY
    def: PRIMARY KEY (training_extra_id) USING BTREE
    table: trainings_extra
    columns:
    - training_extra_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (training_extra_id)
    table: trainings_extra
    columns:
    - training_extra_id
  def: |-
    CREATE TABLE `trainings_extra` (
      `training_extra_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `event_id` int(8) unsigned NOT NULL,
      `training_extra_name` varchar(128) NOT NULL DEFAULT '',
      `training_extra_email` varchar(128) NOT NULL DEFAULT '',
      `training_extra_birthdate` date DEFAULT NULL,
      `training_extra_visible` tinyint(1) NOT NULL DEFAULT 1,
      PRIMARY KEY (`training_extra_id`),
      KEY `event_id` (`event_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: users
  type: BASE TABLE
  columns:
  - name: user_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: username
    type: varchar(64)
    nullable: true
    default: "NULL"
  - name: first_name
    type: varchar(64)
    nullable: false
  - name: last_name
    type: varchar(64)
    nullable: false
  - name: display_name
    type: varchar(64)
    nullable: true
    default: "NULL"
  - name: gender
    type: varchar(32)
    nullable: false
  - name: birthdate
    type: date
    nullable: true
    default: "NULL"
  - name: phone_number
    type: varchar(32)
    nullable: true
    default: "NULL"
  - name: discord_handle
    type: varchar(64)
    nullable: true
    default: "NULL"
  - name: discord_handle_updated
    type: datetime
    nullable: true
    default: "NULL"
  - name: avatar_id
    type: int(4) unsigned
    nullable: true
    default: "NULL"
  - name: privileges
    type: bigint(20)
    nullable: false
    default: "0"
  - name: participation_suspended
    type: text
    nullable: true
    default: "NULL"
  - name: permissions_grants
    type: text
    nullable: true
    default: "NULL"
  - name: permissions_revokes
    type: text
    nullable: true
    default: "NULL"
  - name: permissions_events
    type: text
    nullable: true
    default: "NULL"
  - name: permissions_teams
    type: text
    nullable: true
    default: "NULL"
  - name: challenge
    type: text
    nullable: true
    default: "NULL"
  - name: activated
    type: tinyint(1)
    nullable: false
    default: "0"
  - name: session_token
    type: int(4) unsigned
    nullable: false
    default: "1"
  indexes:
  - name: PRIMARY
    def: PRIMARY KEY (user_id) USING BTREE
    table: users
    columns:
    - user_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (user_id)
    table: users
    columns:
    - user_id
  def: |-
    CREATE TABLE `users` (
      `user_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `username` varchar(64) DEFAULT NULL,
      `first_name` varchar(64) NOT NULL,
      `last_name` varchar(64) NOT NULL,
      `display_name` varchar(64) DEFAULT NULL,
      `gender` varchar(32) NOT NULL,
      `birthdate` date DEFAULT NULL,
      `phone_number` varchar(32) DEFAULT NULL,
      `discord_handle` varchar(64) DEFAULT NULL,
      `discord_handle_updated` datetime DEFAULT NULL,
      `avatar_id` int(4) unsigned DEFAULT NULL,
      `privileges` bigint(20) NOT NULL DEFAULT 0,
      `participation_suspended` text DEFAULT NULL,
      `permissions_grants` text DEFAULT NULL,
      `permissions_revokes` text DEFAULT NULL,
      `permissions_events` text DEFAULT NULL,
      `permissions_teams` text DEFAULT NULL,
      `challenge` text DEFAULT NULL,
      `activated` tinyint(1) NOT NULL DEFAULT 0,
      `session_token` int(4) unsigned NOT NULL DEFAULT 1,
      PRIMARY KEY (`user_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: users_auth
  type: BASE TABLE
  columns:
  - name: user_id
    type: int(8) unsigned
    nullable: false
  - name: auth_type
    type: enum('code','password','passkey')
    nullable: false
  - name: auth_value
    type: text
    nullable: false
  indexes:
  - name: user_id
    def: KEY user_id (user_id) USING BTREE
    table: users_auth
    columns:
    - user_id
  constraints:
  - name: users_auth_user_id
    type: FOREIGN KEY
    def: FOREIGN KEY (user_id) REFERENCES users (user_id)
    table: users_auth
    referencedTable: users
    columns:
    - user_id
    referencedColumns:
    - user_id
  def: |-
    CREATE TABLE `users_auth` (
      `user_id` int(8) unsigned NOT NULL,
      `auth_type` enum('code','password','passkey') NOT NULL,
      `auth_value` text NOT NULL,
      KEY `user_id` (`user_id`),
      CONSTRAINT `users_auth_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`user_id`) ON DELETE CASCADE
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: users_events
  type: BASE TABLE
  columns:
  - name: user_id
    type: int(4) unsigned
    nullable: false
  - name: event_id
    type: int(4) unsigned
    nullable: false
  - name: team_id
    type: int(4) unsigned
    nullable: false
  - name: role_id
    type: int(4) unsigned
    nullable: false
  - name: registration_date
    type: datetime
    nullable: true
    default: "NULL"
  - name: registration_status
    type: enum('Registered','Cancelled','Accepted','Rejected')
    nullable: false
    default: "'Registered'"
  - name: registration_notes
    type: text
    nullable: true
    default: "NULL"
  - name: shirt_fit
    type: enum('Regular','Girly')
    nullable: false
    default: "'Regular'"
  - name: shirt_size
    type: enum('XS','S','M','L','XL','XXL','3XL','4XL')
    nullable: true
    default: "NULL"
  - name: hotel_eligible
    type: tinyint(1) unsigned
    nullable: true
    default: "NULL"
  - name: training_eligible
    type: tinyint(1) unsigned
    nullable: true
    default: "NULL"
  - name: availability_event_limit
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: availability_exceptions
    type: text
    nullable: true
    default: "NULL"
  - name: availability_timeslots
    type: text
    nullable: true
    default: "NULL"
  - name: preference_hours
    type: int(4) unsigned
    nullable: true
    default: "NULL"
  - name: preference_timing_start
    type: int(4) unsigned
    nullable: true
    default: "NULL"
  - name: preference_timing_end
    type: int(4) unsigned
    nullable: true
    default: "NULL"
  - name: preferences
    type: text
    nullable: true
    default: "NULL"
  - name: preferences_dietary
    type: text
    nullable: true
    default: "NULL"
  - name: preferences_updated
    type: datetime
    nullable: true
    default: "NULL"
  - name: fully_available
    type: tinyint(1)
    nullable: false
    default: "1"
  - name: include_credits
    type: tinyint(1)
    nullable: false
    default: "1"
  - name: include_socials
    type: tinyint(1)
    nullable: false
    default: "1"
  indexes:
  - name: users_events_event_id
    def: KEY users_events_event_id (event_id) USING BTREE
    table: users_events
    columns:
    - event_id
  - name: users_events_role_id
    def: KEY users_events_role_id (role_id) USING BTREE
    table: users_events
    columns:
    - role_id
  - name: users_events_team_id
    def: KEY users_events_team_id (team_id) USING BTREE
    table: users_events
    columns:
    - team_id
  - name: user_id
    def: UNIQUE KEY user_id (user_id, event_id, team_id) USING BTREE
    table: users_events
    columns:
    - user_id
    - event_id
    - team_id
  constraints:
  - name: users_events_event_id
    type: FOREIGN KEY
    def: FOREIGN KEY (event_id) REFERENCES events (event_id)
    table: users_events
    referencedTable: events
    columns:
    - event_id
    referencedColumns:
    - event_id
  - name: users_events_role_id
    type: FOREIGN KEY
    def: FOREIGN KEY (role_id) REFERENCES roles (role_id)
    table: users_events
    referencedTable: roles
    columns:
    - role_id
    referencedColumns:
    - role_id
  - name: users_events_team_id
    type: FOREIGN KEY
    def: FOREIGN KEY (team_id) REFERENCES teams (team_id)
    table: users_events
    referencedTable: teams
    columns:
    - team_id
    referencedColumns:
    - team_id
  - name: users_events_user_id
    type: FOREIGN KEY
    def: FOREIGN KEY (user_id) REFERENCES users (user_id)
    table: users_events
    referencedTable: users
    columns:
    - user_id
    referencedColumns:
    - user_id
  - name: user_id
    type: UNIQUE
    def: UNIQUE KEY user_id (user_id, event_id, team_id)
    table: users_events
    columns:
    - user_id
    - event_id
    - team_id
  def: |-
    CREATE TABLE `users_events` (
      `user_id` int(4) unsigned NOT NULL,
      `event_id` int(4) unsigned NOT NULL,
      `team_id` int(4) unsigned NOT NULL,
      `role_id` int(4) unsigned NOT NULL,
      `registration_date` datetime DEFAULT NULL,
      `registration_status` enum('Registered','Cancelled','Accepted','Rejected') NOT NULL DEFAULT 'Registered',
      `registration_notes` text DEFAULT NULL,
      `shirt_fit` enum('Regular','Girly') NOT NULL DEFAULT 'Regular',
      `shirt_size` enum('XS','S','M','L','XL','XXL','3XL','4XL') DEFAULT NULL,
      `hotel_eligible` tinyint(1) unsigned DEFAULT NULL,
      `training_eligible` tinyint(1) unsigned DEFAULT NULL,
      `availability_event_limit` int(8) unsigned DEFAULT NULL,
      `availability_exceptions` text DEFAULT NULL,
      `availability_timeslots` text DEFAULT NULL,
      `preference_hours` int(4) unsigned DEFAULT NULL,
      `preference_timing_start` int(4) unsigned DEFAULT NULL,
      `preference_timing_end` int(4) unsigned DEFAULT NULL,
      `preferences` text DEFAULT NULL,
      `preferences_dietary` text DEFAULT NULL,
      `preferences_updated` datetime DEFAULT NULL,
      `fully_available` tinyint(1) NOT NULL DEFAULT 1,
      `include_credits` tinyint(1) NOT NULL DEFAULT 1,
      `include_socials` tinyint(1) NOT NULL DEFAULT 1,
      UNIQUE KEY `user_id` (`user_id`,`event_id`,`team_id`),
      KEY `users_events_event_id` (`event_id`),
      KEY `users_events_team_id` (`team_id`),
      KEY `users_events_role_id` (`role_id`),
      CONSTRAINT `users_events_event_id` FOREIGN KEY (`event_id`) REFERENCES `events` (`event_id`),
      CONSTRAINT `users_events_role_id` FOREIGN KEY (`role_id`) REFERENCES `roles` (`role_id`),
      CONSTRAINT `users_events_team_id` FOREIGN KEY (`team_id`) REFERENCES `teams` (`team_id`),
      CONSTRAINT `users_events_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`user_id`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: users_settings
  type: BASE TABLE
  columns:
  - name: user_id
    type: int(8) unsigned
    nullable: false
  - name: setting_name
    type: varchar(64)
    nullable: false
  - name: setting_value
    type: text
    nullable: false
  indexes:
  - name: user_id
    def: UNIQUE KEY user_id (user_id, setting_name) USING BTREE
    table: users_settings
    columns:
    - user_id
    - setting_name
  constraints:
  - name: users_settings_user_id
    type: FOREIGN KEY
    def: FOREIGN KEY (user_id) REFERENCES users (user_id)
    table: users_settings
    referencedTable: users
    columns:
    - user_id
    referencedColumns:
    - user_id
  - name: user_id
    type: UNIQUE
    def: UNIQUE KEY user_id (user_id, setting_name)
    table: users_settings
    columns:
    - user_id
    - setting_name
  def: |-
    CREATE TABLE `users_settings` (
      `user_id` int(8) unsigned NOT NULL,
      `setting_name` varchar(64) NOT NULL,
      `setting_value` text NOT NULL,
      UNIQUE KEY `user_id` (`user_id`,`setting_name`),
      CONSTRAINT `users_settings_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`user_id`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: trainings_assignments
  type: BASE TABLE
  columns:
  - name: assignment_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: event_id
    type: int(8) unsigned
    nullable: false
  - name: assignment_user_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: assignment_extra_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: assignment_training_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: assignment_updated
    type: timestamp
    nullable: true
    default: "NULL"
  - name: assignment_confirmed
    type: tinyint(1)
    nullable: false
    default: "0"
  - name: preference_training_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: preference_updated
    type: timestamp
    nullable: true
    default: "NULL"
  indexes:
  - name: assignment_user_id
    def: KEY assignment_user_id (assignment_user_id) USING BTREE
    table: trainings_assignments
    columns:
    - assignment_user_id
  - name: event_id
    def: KEY event_id (event_id) USING BTREE
    table: trainings_assignments
    columns:
    - event_id
  - name: PRIMARY
    def: PRIMARY KEY (assignment_id) USING BTREE
    table: trainings_assignments
    columns:
    - assignment_id
  - name: event_id_2
    def: UNIQUE KEY event_id_2 (event_id, assignment_user_id) USING BTREE
    table: trainings_assignments
    columns:
    - event_id
    - assignment_user_id
  - name: event_id_3
    def: UNIQUE KEY event_id_3 (event_id, assignment_extra_id) USING BTREE
    table: trainings_assignments
    columns:
    - event_id
    - assignment_extra_id
  constraints:
  - name: event_id_2
    type: UNIQUE
    def: UNIQUE KEY event_id_2 (event_id, assignment_user_id)
    table: trainings_assignments
    columns:
    - event_id
    - assignment_user_id
  - name: event_id_3
    type: UNIQUE
    def: UNIQUE KEY event_id_3 (event_id, assignment_extra_id)
    table: trainings_assignments
    columns:
    - event_id
    - assignment_extra_id
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (assignment_id)
    table: trainings_assignments
    columns:
    - assignment_id
  def: |-
    CREATE TABLE `trainings_assignments` (
      `assignment_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `event_id` int(8) unsigned NOT NULL,
      `assignment_user_id` int(8) unsigned DEFAULT NULL,
      `assignment_extra_id` int(8) unsigned DEFAULT NULL,
      `assignment_training_id` int(8) unsigned DEFAULT NULL,
      `assignment_updated` timestamp NULL DEFAULT NULL,
      `assignment_confirmed` tinyint(1) NOT NULL DEFAULT 0,
      `preference_training_id` int(8) unsigned DEFAULT NULL,
      `preference_updated` timestamp NULL DEFAULT NULL,
      PRIMARY KEY (`assignment_id`),
      UNIQUE KEY `event_id_2` (`event_id`,`assignment_user_id`),
      UNIQUE KEY `event_id_3` (`event_id`,`assignment_extra_id`),
      KEY `event_id` (`event_id`),
      KEY `assignment_user_id` (`assignment_user_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: displays_requests
  type: BASE TABLE
  columns:
  - name: request_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: display_id
    type: int(8) unsigned
    nullable: false
  - name: request_event_id
    type: int(8) unsigned
    nullable: false
  - name: request_received_date
    type: datetime
    nullable: false
  - name: request_received_target
    type: enum('Crew','Nardo','Stewards')
    nullable: false
  - name: request_acknowledged_by
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: request_acknowledged_date
    type: datetime
    nullable: true
    default: "NULL"
  - name: request_closed_by
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: request_closed_date
    type: datetime
    nullable: true
    default: "NULL"
  - name: request_closed_reason
    type: text
    nullable: true
    default: "NULL"
  indexes:
  - name: display_id
    def: KEY display_id (display_id) USING BTREE
    table: displays_requests
    columns:
    - display_id
  - name: request_event_id
    def: KEY request_event_id (request_event_id) USING BTREE
    table: displays_requests
    columns:
    - request_event_id
  - name: PRIMARY
    def: PRIMARY KEY (request_id) USING BTREE
    table: displays_requests
    columns:
    - request_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (request_id)
    table: displays_requests
    columns:
    - request_id
  def: |-
    CREATE TABLE `displays_requests` (
      `request_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `display_id` int(8) unsigned NOT NULL,
      `request_event_id` int(8) unsigned NOT NULL,
      `request_received_date` datetime NOT NULL,
      `request_received_target` enum('Crew','Nardo','Stewards') NOT NULL,
      `request_acknowledged_by` int(8) unsigned DEFAULT NULL,
      `request_acknowledged_date` datetime DEFAULT NULL,
      `request_closed_by` int(8) unsigned DEFAULT NULL,
      `request_closed_date` datetime DEFAULT NULL,
      `request_closed_reason` text DEFAULT NULL,
      PRIMARY KEY (`request_id`),
      KEY `display_id` (`display_id`),
      KEY `request_event_id` (`request_event_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: activities_timeslots
  type: BASE TABLE
  columns:
  - name: activity_id
    type: int(8) unsigned
    nullable: false
  - name: timeslot_id
    type: int(8) unsigned
    nullable: false
  - name: timeslot_type
    type: enum('Program','Internal')
    nullable: false
  - name: timeslot_start_time
    type: datetime
    nullable: false
  - name: timeslot_end_time
    type: datetime
    nullable: false
  - name: timeslot_location_id
    type: int(8) unsigned
    nullable: false
  - name: timeslot_created
    type: datetime
    nullable: false
  - name: timeslot_updated
    type: datetime
    nullable: false
  - name: timeslot_deleted
    type: datetime
    nullable: true
    default: "NULL"
  indexes:
  - name: activity_id_2
    def: UNIQUE KEY activity_id_2 (activity_id, timeslot_id) USING BTREE
    table: activities_timeslots
    columns:
    - activity_id
    - timeslot_id
  constraints:
  - name: activity_id_2
    type: UNIQUE
    def: UNIQUE KEY activity_id_2 (activity_id, timeslot_id)
    table: activities_timeslots
    columns:
    - activity_id
    - timeslot_id
  def: |-
    CREATE TABLE `activities_timeslots` (
      `activity_id` int(8) unsigned NOT NULL,
      `timeslot_id` int(8) unsigned NOT NULL,
      `timeslot_type` enum('Program','Internal') NOT NULL,
      `timeslot_start_time` datetime NOT NULL,
      `timeslot_end_time` datetime NOT NULL,
      `timeslot_location_id` int(8) unsigned NOT NULL,
      `timeslot_created` datetime NOT NULL,
      `timeslot_updated` datetime NOT NULL,
      `timeslot_deleted` datetime DEFAULT NULL,
      UNIQUE KEY `activity_id_2` (`activity_id`,`timeslot_id`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: exports_logs
  type: BASE TABLE
  columns:
  - name: export_log_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: export_id
    type: int(8) unsigned
    nullable: false
  - name: access_date
    type: datetime
    nullable: false
    default: current_timestamp()
  - name: access_ip_address
    type: varchar(64)
    nullable: false
  - name: access_user_agent
    type: text
    nullable: false
  - name: access_user_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  indexes:
  - name: export_id
    def: KEY export_id (export_id) USING BTREE
    table: exports_logs
    columns:
    - export_id
  - name: PRIMARY
    def: PRIMARY KEY (export_log_id) USING BTREE
    table: exports_logs
    columns:
    - export_log_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (export_log_id)
    table: exports_logs
    columns:
    - export_log_id
  def: |-
    CREATE TABLE `exports_logs` (
      `export_log_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `export_id` int(8) unsigned NOT NULL,
      `access_date` datetime NOT NULL DEFAULT current_timestamp(),
      `access_ip_address` varchar(64) NOT NULL,
      `access_user_agent` text NOT NULL,
      `access_user_id` int(8) unsigned DEFAULT NULL,
      PRIMARY KEY (`export_log_id`),
      KEY `export_id` (`export_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: users_passkeys
  type: BASE TABLE
  columns:
  - name: user_passkey_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: user_id
    type: int(8) unsigned
    nullable: false
  - name: credential_id
    type: blob
    nullable: false
  - name: credential_rpid
    type: varchar(64)
    nullable: false
  - name: credential_format
    type: text
    nullable: true
    default: "NULL"
  - name: credential_name
    type: text
    nullable: true
    default: "NULL"
  - name: credential_created
    type: datetime
    nullable: false
    default: current_timestamp()
  - name: credential_origin
    type: varchar(64)
    nullable: false
  - name: credential_public_key
    type: blob
    nullable: false
  - name: credential_device_type
    type: text
    nullable: false
  - name: credential_backed_up
    type: tinyint(1)
    nullable: false
  - name: credential_transport
    type: text
    nullable: true
    default: "NULL"
  - name: credential_last_used
    type: datetime
    nullable: true
    default: "NULL"
  - name: counter
    type: bigint(8)
    nullable: false
  indexes:
  - name: user_id
    def: KEY user_id (user_id) USING BTREE
    table: users_passkeys
    columns:
    - user_id
  - name: PRIMARY
    def: PRIMARY KEY (user_passkey_id) USING BTREE
    table: users_passkeys
    columns:
    - user_passkey_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (user_passkey_id)
    table: users_passkeys
    columns:
    - user_passkey_id
  def: |-
    CREATE TABLE `users_passkeys` (
      `user_passkey_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `user_id` int(8) unsigned NOT NULL,
      `credential_id` blob NOT NULL,
      `credential_rpid` varchar(64) NOT NULL,
      `credential_format` text DEFAULT NULL,
      `credential_name` text DEFAULT NULL,
      `credential_created` datetime NOT NULL DEFAULT current_timestamp(),
      `credential_origin` varchar(64) NOT NULL,
      `credential_public_key` blob NOT NULL,
      `credential_device_type` text NOT NULL,
      `credential_backed_up` tinyint(1) NOT NULL,
      `credential_transport` text DEFAULT NULL,
      `credential_last_used` datetime DEFAULT NULL,
      `counter` bigint(8) NOT NULL,
      PRIMARY KEY (`user_passkey_id`),
      KEY `user_id` (`user_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: retention
  type: BASE TABLE
  columns:
  - name: user_id
    type: int(8) unsigned
    nullable: false
  - name: event_id
    type: int(8) unsigned
    nullable: false
  - name: team_id
    type: int(8) unsigned
    nullable: false
  - name: retention_status
    type: enum('Contacting','Declined')
    nullable: true
    default: "NULL"
  - name: retention_assignee_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: retention_notes
    type: text
    nullable: true
    default: "NULL"
  indexes:
  - name: PRIMARY
    def: PRIMARY KEY (user_id, event_id, team_id) USING BTREE
    table: retention
    columns:
    - user_id
    - event_id
    - team_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (user_id, event_id, team_id)
    table: retention
    columns:
    - user_id
    - event_id
    - team_id
  def: |-
    CREATE TABLE `retention` (
      `user_id` int(8) unsigned NOT NULL,
      `event_id` int(8) unsigned NOT NULL,
      `team_id` int(8) unsigned NOT NULL,
      `retention_status` enum('Contacting','Declined') DEFAULT NULL,
      `retention_assignee_id` int(8) unsigned DEFAULT NULL,
      `retention_notes` text DEFAULT NULL,
      PRIMARY KEY (`user_id`,`event_id`,`team_id`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: teams
  type: BASE TABLE
  columns:
  - name: team_id
    type: int(4) unsigned
    nullable: false
    extraDef: auto_increment
  - name: team_slug
    type: varchar(16)
    nullable: false
  - name: team_name
    type: varchar(32)
    nullable: false
  - name: team_plural
    type: varchar(32)
    nullable: false
  - name: team_title
    type: varchar(128)
    nullable: false
  - name: team_description
    type: text
    nullable: false
  - name: team_environment
    type: varchar(32)
    nullable: false
  - name: team_environment_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: team_invite_key
    type: varchar(32)
    nullable: false
  - name: team_flag_enable_scheduling
    type: tinyint(1) unsigned
    nullable: false
    default: "0"
  - name: team_flag_manages_content
    type: tinyint(1) unsigned
    nullable: false
    default: "0"
  - name: team_flag_manages_faq
    type: tinyint(1) unsigned
    nullable: false
    default: "0"
  - name: team_flag_manages_first_aid
    type: tinyint(1) unsigned
    nullable: false
    default: "0"
  - name: team_flag_manages_security
    type: tinyint(1) unsigned
    nullable: false
    default: "0"
  - name: team_flag_request_confirmation
    type: tinyint(1) unsigned
    nullable: false
    default: "0"
  - name: team_request_confirmation
    type: tinyint(1) unsigned
    nullable: false
    default: "0"
  - name: team_manages_faq
    type: tinyint(1) unsigned
    nullable: false
    default: "0"
  - name: team_manages_first_aid
    type: tinyint(1) unsigned
    nullable: false
    default: "0"
  - name: team_manages_security
    type: tinyint(1) unsigned
    nullable: false
    default: "0"
  - name: team_colour_dark_theme
    type: varchar(16)
    nullable: false
  - name: team_colour_light_theme
    type: varchar(16)
    nullable: false
  - name: team_deleted
    type: datetime
    nullable: true
    default: "NULL"
  indexes:
  - name: team_slug
    def: KEY team_slug (team_slug) USING BTREE
    table: teams
    columns:
    - team_slug
  - name: PRIMARY
    def: PRIMARY KEY (team_id) USING BTREE
    table: teams
    columns:
    - team_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (team_id)
    table: teams
    columns:
    - team_id
  def: |-
    CREATE TABLE `teams` (
      `team_id` int(4) unsigned NOT NULL AUTO_INCREMENT,
      `team_slug` varchar(16) NOT NULL,
      `team_name` varchar(32) NOT NULL,
      `team_plural` varchar(32) NOT NULL,
      `team_title` varchar(128) NOT NULL,
      `team_description` text NOT NULL,
      `team_environment` varchar(32) NOT NULL,
      `team_environment_id` int(8) unsigned DEFAULT NULL,
      `team_invite_key` varchar(32) NOT NULL,
      `team_flag_enable_scheduling` tinyint(1) unsigned NOT NULL DEFAULT 0,
      `team_flag_manages_content` tinyint(1) unsigned NOT NULL DEFAULT 0,
      `team_flag_manages_faq` tinyint(1) unsigned NOT NULL DEFAULT 0,
      `team_flag_manages_first_aid` tinyint(1) unsigned NOT NULL DEFAULT 0,
      `team_flag_manages_security` tinyint(1) unsigned NOT NULL DEFAULT 0,
      `team_flag_request_confirmation` tinyint(1) unsigned NOT NULL DEFAULT 0,
      `team_request_confirmation` tinyint(1) unsigned NOT NULL DEFAULT 0,
      `team_manages_faq` tinyint(1) unsigned NOT NULL DEFAULT 0,
      `team_manages_first_aid` tinyint(1) unsigned NOT NULL DEFAULT 0,
      `team_manages_security` tinyint(1) unsigned NOT NULL DEFAULT 0,
      `team_colour_dark_theme` varchar(16) NOT NULL,
      `team_colour_light_theme` varchar(16) NOT NULL,
      `team_deleted` datetime DEFAULT NULL,
      PRIMARY KEY (`team_id`),
      KEY `team_slug` (`team_slug`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: hotels_preferences
  type: BASE TABLE
  columns:
  - name: user_id
    type: int(8) unsigned
    nullable: false
  - name: event_id
    type: int(8) unsigned
    nullable: false
  - name: team_id
    type: int(8) unsigned
    nullable: false
  - name: hotel_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: hotel_date_check_in
    type: date
    nullable: true
    default: "NULL"
  - name: hotel_date_check_out
    type: date
    nullable: true
    default: "NULL"
  - name: hotel_sharing_people
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: hotel_sharing_preferences
    type: text
    nullable: true
    default: "NULL"
  - name: hotel_preferences_updated
    type: datetime
    nullable: false
    default: current_timestamp()
  indexes:
  - name: user_id
    def: UNIQUE KEY user_id (user_id, event_id, team_id) USING BTREE
    table: hotels_preferences
    columns:
    - user_id
    - event_id
    - team_id
  constraints:
  - name: user_id
    type: UNIQUE
    def: UNIQUE KEY user_id (user_id, event_id, team_id)
    table: hotels_preferences
    columns:
    - user_id
    - event_id
    - team_id
  def: |-
    CREATE TABLE `hotels_preferences` (
      `user_id` int(8) unsigned NOT NULL,
      `event_id` int(8) unsigned NOT NULL,
      `team_id` int(8) unsigned NOT NULL,
      `hotel_id` int(8) unsigned DEFAULT NULL,
      `hotel_date_check_in` date DEFAULT NULL,
      `hotel_date_check_out` date DEFAULT NULL,
      `hotel_sharing_people` int(8) unsigned DEFAULT NULL,
      `hotel_sharing_preferences` text DEFAULT NULL,
      `hotel_preferences_updated` datetime NOT NULL DEFAULT current_timestamp(),
      UNIQUE KEY `user_id` (`user_id`,`event_id`,`team_id`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: outbox
  type: BASE TABLE
  columns:
  - name: outbox_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: outbox_timestamp
    type: timestamp
    nullable: false
    default: current_timestamp()
  - name: outbox_sender
    type: varchar(128)
    nullable: false
  - name: outbox_sender_user_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: outbox_to
    type: varchar(128)
    nullable: false
  - name: outbox_to_user_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: outbox_cc
    type: varchar(128)
    nullable: true
    default: "NULL"
  - name: outbox_bcc
    type: varchar(128)
    nullable: true
    default: "NULL"
  - name: outbox_headers
    type: text
    nullable: true
    default: "NULL"
  - name: outbox_subject
    type: text
    nullable: false
  - name: outbox_body_html
    type: text
    nullable: false
    default: "''"
  - name: outbox_body_text
    type: text
    nullable: false
    default: "''"
  - name: outbox_logs
    type: text
    nullable: true
    default: "NULL"
  - name: outbox_error_name
    type: varchar(32)
    nullable: true
    default: "NULL"
  - name: outbox_error_cause
    type: text
    nullable: true
    default: "NULL"
  - name: outbox_error_message
    type: text
    nullable: true
    default: "NULL"
  - name: outbox_error_stack
    type: text
    nullable: true
    default: "NULL"
  - name: outbox_result_message_id
    type: varchar(256)
    nullable: true
    default: "NULL"
  - name: outbox_result_accepted
    type: text
    nullable: true
    default: "NULL"
  - name: outbox_result_rejected
    type: text
    nullable: true
    default: "NULL"
  - name: outbox_result_pending
    type: text
    nullable: true
    default: "NULL"
  - name: outbox_result_response
    type: text
    nullable: true
    default: "NULL"
  indexes:
  - name: outbox_date
    def: KEY outbox_date (outbox_timestamp) USING BTREE
    table: outbox
    columns:
    - outbox_timestamp
  - name: outbox_sender_user_id
    def: KEY outbox_sender_user_id (outbox_sender_user_id) USING BTREE
    table: outbox
    columns:
    - outbox_sender_user_id
  - name: outbox_to_user_id
    def: KEY outbox_to_user_id (outbox_to_user_id) USING BTREE
    table: outbox
    columns:
    - outbox_to_user_id
  - name: PRIMARY
    def: PRIMARY KEY (outbox_id) USING BTREE
    table: outbox
    columns:
    - outbox_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (outbox_id)
    table: outbox
    columns:
    - outbox_id
  def: |-
    CREATE TABLE `outbox` (
      `outbox_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `outbox_timestamp` timestamp NOT NULL DEFAULT current_timestamp(),
      `outbox_sender` varchar(128) NOT NULL,
      `outbox_sender_user_id` int(8) unsigned DEFAULT NULL,
      `outbox_to` varchar(128) NOT NULL,
      `outbox_to_user_id` int(8) unsigned DEFAULT NULL,
      `outbox_cc` varchar(128) DEFAULT NULL,
      `outbox_bcc` varchar(128) DEFAULT NULL,
      `outbox_headers` text DEFAULT NULL,
      `outbox_subject` text NOT NULL,
      `outbox_body_html` text NOT NULL DEFAULT '',
      `outbox_body_text` text NOT NULL DEFAULT '',
      `outbox_logs` text DEFAULT NULL,
      `outbox_error_name` varchar(32) DEFAULT NULL,
      `outbox_error_cause` text DEFAULT NULL,
      `outbox_error_message` text DEFAULT NULL,
      `outbox_error_stack` text DEFAULT NULL,
      `outbox_result_message_id` varchar(256) DEFAULT NULL,
      `outbox_result_accepted` text DEFAULT NULL,
      `outbox_result_rejected` text DEFAULT NULL,
      `outbox_result_pending` text DEFAULT NULL,
      `outbox_result_response` text DEFAULT NULL,
      PRIMARY KEY (`outbox_id`),
      KEY `outbox_date` (`outbox_timestamp`),
      KEY `outbox_to_user_id` (`outbox_to_user_id`),
      KEY `outbox_sender_user_id` (`outbox_sender_user_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: nardo
  type: BASE TABLE
  columns:
  - name: nardo_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: nardo_advice
    type: text
    nullable: false
  - name: nardo_author_id
    type: int(8) unsigned
    nullable: false
  - name: nardo_author_date
    type: timestamp
    nullable: false
    default: current_timestamp()
    extraDef: on update current_timestamp()
  - name: nardo_visible
    type: tinyint(1)
    nullable: false
    default: "1"
  indexes:
  - name: PRIMARY
    def: PRIMARY KEY (nardo_id) USING BTREE
    table: nardo
    columns:
    - nardo_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (nardo_id)
    table: nardo
    columns:
    - nardo_id
  def: |-
    CREATE TABLE `nardo` (
      `nardo_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `nardo_advice` text NOT NULL,
      `nardo_author_id` int(8) unsigned NOT NULL,
      `nardo_author_date` timestamp NOT NULL DEFAULT current_timestamp() ON UPDATE current_timestamp(),
      `nardo_visible` tinyint(1) NOT NULL DEFAULT 1,
      PRIMARY KEY (`nardo_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: feedback
  type: BASE TABLE
  columns:
  - name: feedback_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: user_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: feedback_name
    type: text
    nullable: true
    default: "NULL"
  - name: feedback_date
    type: datetime
    nullable: false
  - name: feedback_text
    type: text
    nullable: false
  indexes:
  - name: PRIMARY
    def: PRIMARY KEY (feedback_id) USING BTREE
    table: feedback
    columns:
    - feedback_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (feedback_id)
    table: feedback
    columns:
    - feedback_id
  def: |-
    CREATE TABLE `feedback` (
      `feedback_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `user_id` int(8) unsigned DEFAULT NULL,
      `feedback_name` text DEFAULT NULL,
      `feedback_date` datetime NOT NULL,
      `feedback_text` text NOT NULL,
      PRIMARY KEY (`feedback_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: activities
  type: BASE TABLE
  columns:
  - name: activity_id
    type: int(8) unsigned
    nullable: false
  - name: activity_festival_id
    type: int(8) unsigned
    nullable: false
  - name: activity_type
    type: enum('Program','Internal')
    nullable: false
  - name: activity_title
    type: text
    nullable: false
  - name: activity_description
    type: text
    nullable: true
    default: "NULL"
  - name: activity_description_web
    type: text
    nullable: true
    default: "NULL"
  - name: activity_rules
    type: text
    nullable: true
    default: "NULL"
  - name: activity_url
    type: text
    nullable: true
    default: "NULL"
  - name: activity_price
    type: double
    nullable: true
    default: "NULL"
  - name: activity_help_needed
    type: tinyint(1)
    nullable: false
  - name: activity_max_visitors
    type: int(8)
    nullable: true
    default: "NULL"
  - name: activity_location_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
    comment: Only for internal activities
  - name: activity_type_adults_only
    type: tinyint(1)
    nullable: false
  - name: activity_type_competition
    type: tinyint(1)
    nullable: false
  - name: activity_type_cosplay
    type: tinyint(1)
    nullable: false
  - name: activity_type_event
    type: tinyint(1)
    nullable: false
  - name: activity_type_game_room
    type: tinyint(1)
    nullable: false
  - name: activity_type_video
    type: tinyint(1)
    nullable: false
  - name: activity_visible
    type: tinyint(1)
    nullable: false
  - name: activity_visible_reason
    type: text
    nullable: true
    default: "NULL"
  - name: activity_request_assignee
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: activity_request_notes
    type: text
    nullable: true
    default: "NULL"
  - name: activity_created
    type: datetime
    nullable: false
  - name: activity_updated
    type: datetime
    nullable: false
  - name: activity_deleted
    type: datetime
    nullable: true
    default: "NULL"
  indexes:
  - name: PRIMARY
    def: PRIMARY KEY (activity_id) USING BTREE
    table: activities
    columns:
    - activity_id
  - name: activity_id
    def: UNIQUE KEY activity_id (activity_id, activity_festival_id) USING BTREE
    table: activities
    columns:
    - activity_id
    - activity_festival_id
  constraints:
  - name: activity_id
    type: UNIQUE
    def: UNIQUE KEY activity_id (activity_id, activity_festival_id)
    table: activities
    columns:
    - activity_id
    - activity_festival_id
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (activity_id)
    table: activities
    columns:
    - activity_id
  def: |-
    CREATE TABLE `activities` (
      `activity_id` int(8) unsigned NOT NULL,
      `activity_festival_id` int(8) unsigned NOT NULL,
      `activity_type` enum('Program','Internal') NOT NULL,
      `activity_title` text NOT NULL,
      `activity_description` text DEFAULT NULL,
      `activity_description_web` text DEFAULT NULL,
      `activity_rules` text DEFAULT NULL,
      `activity_url` text DEFAULT NULL,
      `activity_price` double DEFAULT NULL,
      `activity_help_needed` tinyint(1) NOT NULL,
      `activity_max_visitors` int(8) DEFAULT NULL,
      `activity_location_id` int(8) unsigned DEFAULT NULL COMMENT 'Only for internal activities',
      `activity_type_adults_only` tinyint(1) NOT NULL,
      `activity_type_competition` tinyint(1) NOT NULL,
      `activity_type_cosplay` tinyint(1) NOT NULL,
      `activity_type_event` tinyint(1) NOT NULL,
      `activity_type_game_room` tinyint(1) NOT NULL,
      `activity_type_video` tinyint(1) NOT NULL,
      `activity_visible` tinyint(1) NOT NULL,
      `activity_visible_reason` text DEFAULT NULL,
      `activity_request_assignee` int(8) unsigned DEFAULT NULL,
      `activity_request_notes` text DEFAULT NULL,
      `activity_created` datetime NOT NULL,
      `activity_updated` datetime NOT NULL,
      `activity_deleted` datetime DEFAULT NULL,
      PRIMARY KEY (`activity_id`),
      UNIQUE KEY `activity_id` (`activity_id`,`activity_festival_id`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: activities_logs
  type: BASE TABLE
  columns:
  - name: mutation_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: festival_id
    type: int(8) unsigned
    nullable: false
  - name: activity_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: area_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: timeslot_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: location_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: mutation
    type: enum('Created','Updated','Deleted')
    nullable: false
  - name: mutation_fields
    type: text
    nullable: true
    default: "NULL"
  - name: mutation_severity
    type: enum('Low','Moderate','Important')
    nullable: false
  - name: mutation_user_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: mutation_date
    type: datetime
    nullable: false
  indexes:
  - name: activity_id
    def: KEY activity_id (activity_id) USING BTREE
    table: activities_logs
    columns:
    - activity_id
  - name: area_id
    def: KEY area_id (area_id) USING BTREE
    table: activities_logs
    columns:
    - area_id
  - name: festival_id
    def: KEY festival_id (festival_id) USING BTREE
    table: activities_logs
    columns:
    - festival_id
  - name: location_id
    def: KEY location_id (location_id) USING BTREE
    table: activities_logs
    columns:
    - location_id
  - name: timeslot_id
    def: KEY timeslot_id (timeslot_id) USING BTREE
    table: activities_logs
    columns:
    - timeslot_id
  - name: PRIMARY
    def: PRIMARY KEY (mutation_id) USING BTREE
    table: activities_logs
    columns:
    - mutation_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (mutation_id)
    table: activities_logs
    columns:
    - mutation_id
  def: |-
    CREATE TABLE `activities_logs` (
      `mutation_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `festival_id` int(8) unsigned NOT NULL,
      `activity_id` int(8) unsigned DEFAULT NULL,
      `area_id` int(8) unsigned DEFAULT NULL,
      `timeslot_id` int(8) unsigned DEFAULT NULL,
      `location_id` int(8) unsigned DEFAULT NULL,
      `mutation` enum('Created','Updated','Deleted') NOT NULL,
      `mutation_fields` text DEFAULT NULL,
      `mutation_severity` enum('Low','Moderate','Important') NOT NULL,
      `mutation_user_id` int(8) unsigned DEFAULT NULL,
      `mutation_date` datetime NOT NULL,
      PRIMARY KEY (`mutation_id`),
      KEY `festival_id` (`festival_id`),
      KEY `activity_id` (`activity_id`),
      KEY `timeslot_id` (`timeslot_id`),
      KEY `location_id` (`location_id`),
      KEY `area_id` (`area_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: shifts_categories
  type: BASE TABLE
  columns:
  - name: shift_category_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: shift_category_name
    type: varchar(32)
    nullable: false
  - name: shift_category_colour
    type: varchar(32)
    nullable: false
  - name: shift_category_excitement
    type: double
    nullable: false
  - name: shift_category_count_contribution
    type: tinyint(1)
    nullable: false
  - name: shift_category_order
    type: int(8)
    nullable: false
  - name: shift_category_deleted
    type: datetime
    nullable: true
    default: "NULL"
  indexes:
  - name: PRIMARY
    def: PRIMARY KEY (shift_category_id) USING BTREE
    table: shifts_categories
    columns:
    - shift_category_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (shift_category_id)
    table: shifts_categories
    columns:
    - shift_category_id
  def: |-
    CREATE TABLE `shifts_categories` (
      `shift_category_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `shift_category_name` varchar(32) NOT NULL,
      `shift_category_colour` varchar(32) NOT NULL,
      `shift_category_excitement` double NOT NULL,
      `shift_category_count_contribution` tinyint(1) NOT NULL,
      `shift_category_order` int(8) NOT NULL,
      `shift_category_deleted` datetime DEFAULT NULL,
      PRIMARY KEY (`shift_category_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: hotels_bookings
  type: BASE TABLE
  columns:
  - name: booking_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: event_id
    type: int(8) unsigned
    nullable: false
  - name: booking_hotel_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: booking_check_in
    type: date
    nullable: false
    default: current_timestamp()
  - name: booking_check_out
    type: date
    nullable: false
    default: current_timestamp()
  - name: booking_confirmed
    type: tinyint(1)
    nullable: false
    default: "0"
  - name: booking_visible
    type: tinyint(1)
    nullable: false
    default: "1"
  indexes:
  - name: event_id
    def: KEY event_id (event_id) USING BTREE
    table: hotels_bookings
    columns:
    - event_id
  - name: PRIMARY
    def: PRIMARY KEY (booking_id) USING BTREE
    table: hotels_bookings
    columns:
    - booking_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (booking_id)
    table: hotels_bookings
    columns:
    - booking_id
  def: |-
    CREATE TABLE `hotels_bookings` (
      `booking_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `event_id` int(8) unsigned NOT NULL,
      `booking_hotel_id` int(8) unsigned DEFAULT NULL,
      `booking_check_in` date NOT NULL DEFAULT current_timestamp(),
      `booking_check_out` date NOT NULL DEFAULT current_timestamp(),
      `booking_confirmed` tinyint(1) NOT NULL DEFAULT 0,
      `booking_visible` tinyint(1) NOT NULL DEFAULT 1,
      PRIMARY KEY (`booking_id`),
      KEY `event_id` (`event_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: hotels_assignments
  type: BASE TABLE
  columns:
  - name: assignment_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: booking_id
    type: int(8) unsigned
    nullable: false
  - name: event_id
    type: int(8) unsigned
    nullable: false
  - name: assignment_user_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: assignment_name
    type: varchar(64)
    nullable: true
    default: "NULL"
  - name: assignment_primary
    type: tinyint(1)
    nullable: false
    default: "0"
  - name: assignment_created
    type: datetime
    nullable: false
    default: current_timestamp()
  indexes:
  - name: assignment_user_id
    def: KEY assignment_user_id (assignment_user_id) USING BTREE
    table: hotels_assignments
    columns:
    - assignment_user_id
  - name: booking_id
    def: KEY booking_id (booking_id) USING BTREE
    table: hotels_assignments
    columns:
    - booking_id
  - name: event_id
    def: KEY event_id (event_id) USING BTREE
    table: hotels_assignments
    columns:
    - event_id
  - name: PRIMARY
    def: PRIMARY KEY (assignment_id) USING BTREE
    table: hotels_assignments
    columns:
    - assignment_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (assignment_id)
    table: hotels_assignments
    columns:
    - assignment_id
  def: |-
    CREATE TABLE `hotels_assignments` (
      `assignment_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `booking_id` int(8) unsigned NOT NULL,
      `event_id` int(8) unsigned NOT NULL,
      `assignment_user_id` int(8) unsigned DEFAULT NULL,
      `assignment_name` varchar(64) DEFAULT NULL,
      `assignment_primary` tinyint(1) NOT NULL DEFAULT 0,
      `assignment_created` datetime NOT NULL DEFAULT current_timestamp(),
      PRIMARY KEY (`assignment_id`),
      KEY `assignment_user_id` (`assignment_user_id`),
      KEY `booking_id` (`booking_id`),
      KEY `event_id` (`event_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: activities_locations
  type: BASE TABLE
  columns:
  - name: location_id
    type: int(8) unsigned
    nullable: false
  - name: location_festival_id
    type: int(8) unsigned
    nullable: false
  - name: location_type
    type: enum('Program','Internal')
    nullable: false
  - name: location_name
    type: text
    nullable: false
  - name: location_display_name
    type: text
    nullable: true
    default: "NULL"
  - name: location_area_id
    type: int(8) unsigned
    nullable: false
  - name: location_created
    type: datetime
    nullable: false
  - name: location_updated
    type: datetime
    nullable: false
  - name: location_deleted
    type: datetime
    nullable: true
    default: "NULL"
  indexes:
  - name: PRIMARY
    def: PRIMARY KEY (location_id, location_festival_id) USING BTREE
    table: activities_locations
    columns:
    - location_id
    - location_festival_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (location_id, location_festival_id)
    table: activities_locations
    columns:
    - location_id
    - location_festival_id
  def: |-
    CREATE TABLE `activities_locations` (
      `location_id` int(8) unsigned NOT NULL,
      `location_festival_id` int(8) unsigned NOT NULL,
      `location_type` enum('Program','Internal') NOT NULL,
      `location_name` text NOT NULL,
      `location_display_name` text DEFAULT NULL,
      `location_area_id` int(8) unsigned NOT NULL,
      `location_created` datetime NOT NULL,
      `location_updated` datetime NOT NULL,
      `location_deleted` datetime DEFAULT NULL,
      PRIMARY KEY (`location_id`,`location_festival_id`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: error_logs
  type: BASE TABLE
  columns:
  - name: error_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: error_date
    type: datetime
    nullable: false
  - name: error_user_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: error_ip_address
    type: varchar(64)
    nullable: true
    default: "NULL"
  - name: error_origin
    type: varchar(64)
    nullable: false
  - name: error_pathname
    type: text
    nullable: false
  - name: error_name
    type: text
    nullable: false
  - name: error_message
    type: text
    nullable: false
  - name: error_stack
    type: text
    nullable: true
    default: "NULL"
  - name: error_digest
    type: text
    nullable: true
    default: "NULL"
  indexes:
  - name: PRIMARY
    def: PRIMARY KEY (error_id) USING BTREE
    table: error_logs
    columns:
    - error_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (error_id)
    table: error_logs
    columns:
    - error_id
  def: |-
    CREATE TABLE `error_logs` (
      `error_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `error_date` datetime NOT NULL,
      `error_user_id` int(8) unsigned DEFAULT NULL,
      `error_ip_address` varchar(64) DEFAULT NULL,
      `error_origin` varchar(64) NOT NULL,
      `error_pathname` text NOT NULL,
      `error_name` text NOT NULL,
      `error_message` text NOT NULL,
      `error_stack` text DEFAULT NULL,
      `error_digest` text DEFAULT NULL,
      PRIMARY KEY (`error_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: schedule_logs
  type: BASE TABLE
  columns:
  - name: mutation_id
    type: int(4) unsigned
    nullable: false
    extraDef: auto_increment
  - name: event_id
    type: int(4) unsigned
    nullable: false
  - name: schedule_id
    type: int(4) unsigned
    nullable: false
  - name: mutation
    type: enum('Created','Updated','Deleted')
    nullable: false
  - name: mutation_before_shift_id
    type: int(4) unsigned
    nullable: true
    default: "NULL"
  - name: mutation_before_time_start
    type: datetime
    nullable: true
    default: "NULL"
  - name: mutation_before_time_end
    type: datetime
    nullable: true
    default: "NULL"
  - name: mutation_before_user_id
    type: int(4) unsigned
    nullable: true
    default: "NULL"
  - name: mutation_after_shift_id
    type: int(4) unsigned
    nullable: true
    default: "NULL"
  - name: mutation_after_time_start
    type: datetime
    nullable: true
    default: "NULL"
  - name: mutation_after_time_end
    type: datetime
    nullable: true
    default: "NULL"
  - name: mutation_after_user_id
    type: int(4) unsigned
    nullable: true
    default: "NULL"
  - name: mutation_user_id
    type: int(4) unsigned
    nullable: false
  - name: mutation_date
    type: datetime
    nullable: false
    default: current_timestamp()
  indexes:
  - name: event_id
    def: KEY event_id (event_id) USING BTREE
    table: schedule_logs
    columns:
    - event_id
  - name: schedule_id
    def: KEY schedule_id (schedule_id) USING BTREE
    table: schedule_logs
    columns:
    - schedule_id
  - name: PRIMARY
    def: PRIMARY KEY (mutation_id) USING BTREE
    table: schedule_logs
    columns:
    - mutation_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (mutation_id)
    table: schedule_logs
    columns:
    - mutation_id
  def: |-
    CREATE TABLE `schedule_logs` (
      `mutation_id` int(4) unsigned NOT NULL AUTO_INCREMENT,
      `event_id` int(4) unsigned NOT NULL,
      `schedule_id` int(4) unsigned NOT NULL,
      `mutation` enum('Created','Updated','Deleted') NOT NULL,
      `mutation_before_shift_id` int(4) unsigned DEFAULT NULL,
      `mutation_before_time_start` datetime DEFAULT NULL,
      `mutation_before_time_end` datetime DEFAULT NULL,
      `mutation_before_user_id` int(4) unsigned DEFAULT NULL,
      `mutation_after_shift_id` int(4) unsigned DEFAULT NULL,
      `mutation_after_time_start` datetime DEFAULT NULL,
      `mutation_after_time_end` datetime DEFAULT NULL,
      `mutation_after_user_id` int(4) unsigned DEFAULT NULL,
      `mutation_user_id` int(4) unsigned NOT NULL,
      `mutation_date` datetime NOT NULL DEFAULT current_timestamp(),
      PRIMARY KEY (`mutation_id`),
      KEY `schedule_id` (`schedule_id`),
      KEY `event_id` (`event_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: activities_areas
  type: BASE TABLE
  columns:
  - name: area_id
    type: int(8) unsigned
    nullable: false
  - name: area_festival_id
    type: int(8) unsigned
    nullable: false
  - name: area_type
    type: enum('Program','Internal')
    nullable: false
  - name: area_name
    type: text
    nullable: false
  - name: area_display_name
    type: text
    nullable: true
    default: "NULL"
  - name: area_created
    type: datetime
    nullable: false
  - name: area_updated
    type: datetime
    nullable: false
  - name: area_deleted
    type: datetime
    nullable: true
    default: "NULL"
  indexes:
  - name: PRIMARY
    def: PRIMARY KEY (area_id, area_festival_id) USING BTREE
    table: activities_areas
    columns:
    - area_id
    - area_festival_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (area_id, area_festival_id)
    table: activities_areas
    columns:
    - area_id
    - area_festival_id
  def: |-
    CREATE TABLE `activities_areas` (
      `area_id` int(8) unsigned NOT NULL,
      `area_festival_id` int(8) unsigned NOT NULL,
      `area_type` enum('Program','Internal') NOT NULL,
      `area_name` text NOT NULL,
      `area_display_name` text DEFAULT NULL,
      `area_created` datetime NOT NULL,
      `area_updated` datetime NOT NULL,
      `area_deleted` datetime DEFAULT NULL,
      PRIMARY KEY (`area_id`,`area_festival_id`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: outbox_twilio
  type: BASE TABLE
  columns:
  - name: outbox_twilio_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: outbox_timestamp
    type: datetime
    nullable: false
  - name: outbox_type
    type: enum('SMS','WhatsApp')
    nullable: false
  - name: outbox_sender
    type: text
    nullable: true
    default: "NULL"
  - name: outbox_sender_user_id
    type: int(8)
    nullable: true
    default: "NULL"
  - name: outbox_recipient
    type: text
    nullable: false
  - name: outbox_recipient_user_id
    type: int(8)
    nullable: false
  - name: outbox_message
    type: text
    nullable: false
  - name: outbox_error_name
    type: varchar(32)
    nullable: true
    default: "NULL"
  - name: outbox_error_cause
    type: text
    nullable: true
    default: "NULL"
  - name: outbox_error_message
    type: text
    nullable: true
    default: "NULL"
  - name: outbox_error_stack
    type: text
    nullable: true
    default: "NULL"
  - name: outbox_result_status
    type: varchar(32)
    nullable: true
    default: "NULL"
  - name: outbox_result_sid
    type: varchar(64)
    nullable: true
    default: "NULL"
  - name: outbox_result_time
    type: int(11)
    nullable: true
    default: "NULL"
  - name: outbox_result_error_code
    type: int(8)
    nullable: true
    default: "NULL"
  - name: outbox_result_error_message
    type: text
    nullable: true
    default: "NULL"
  indexes:
  - name: outbox_result_sid
    def: KEY outbox_result_sid (outbox_result_sid) USING BTREE
    table: outbox_twilio
    columns:
    - outbox_result_sid
  - name: PRIMARY
    def: PRIMARY KEY (outbox_twilio_id) USING BTREE
    table: outbox_twilio
    columns:
    - outbox_twilio_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (outbox_twilio_id)
    table: outbox_twilio
    columns:
    - outbox_twilio_id
  def: |-
    CREATE TABLE `outbox_twilio` (
      `outbox_twilio_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `outbox_timestamp` datetime NOT NULL,
      `outbox_type` enum('SMS','WhatsApp') NOT NULL,
      `outbox_sender` text DEFAULT NULL,
      `outbox_sender_user_id` int(8) DEFAULT NULL,
      `outbox_recipient` text NOT NULL,
      `outbox_recipient_user_id` int(8) NOT NULL,
      `outbox_message` text NOT NULL,
      `outbox_error_name` varchar(32) DEFAULT NULL,
      `outbox_error_cause` text DEFAULT NULL,
      `outbox_error_message` text DEFAULT NULL,
      `outbox_error_stack` text DEFAULT NULL,
      `outbox_result_status` varchar(32) DEFAULT NULL,
      `outbox_result_sid` varchar(64) DEFAULT NULL,
      `outbox_result_time` int(11) DEFAULT NULL,
      `outbox_result_error_code` int(8) DEFAULT NULL,
      `outbox_result_error_message` text DEFAULT NULL,
      PRIMARY KEY (`outbox_twilio_id`),
      KEY `outbox_result_sid` (`outbox_result_sid`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: subscriptions_publications
  type: BASE TABLE
  columns:
  - name: publication_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: publication_user_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: publication_subscription_type
    type: enum('Application','Help','Registration','Test')
    nullable: false
  - name: publication_subscription_type_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: publication_created
    type: datetime
    nullable: false
  indexes:
  - name: PRIMARY
    def: PRIMARY KEY (publication_id) USING BTREE
    table: subscriptions_publications
    columns:
    - publication_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (publication_id)
    table: subscriptions_publications
    columns:
    - publication_id
  def: |-
    CREATE TABLE `subscriptions_publications` (
      `publication_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `publication_user_id` int(8) unsigned DEFAULT NULL,
      `publication_subscription_type` enum('Application','Help','Registration','Test') NOT NULL,
      `publication_subscription_type_id` int(8) unsigned DEFAULT NULL,
      `publication_created` datetime NOT NULL,
      PRIMARY KEY (`publication_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: subscriptions
  type: BASE TABLE
  columns:
  - name: subscription_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: subscription_user_id
    type: int(8) unsigned
    nullable: false
  - name: subscription_type
    type: enum('Application','Help','Registration','Test')
    nullable: false
  - name: subscription_type_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: subscription_channel_email
    type: tinyint(1) unsigned
    nullable: false
  - name: subscription_channel_notification
    type: tinyint(1) unsigned
    nullable: false
  - name: subscription_channel_sms
    type: tinyint(1) unsigned
    nullable: false
  - name: subscription_channel_whatsapp
    type: tinyint(1) unsigned
    nullable: false
  indexes:
  - name: whatsapp_user_id
    def: KEY whatsapp_user_id (subscription_user_id) USING BTREE
    table: subscriptions
    columns:
    - subscription_user_id
  - name: PRIMARY
    def: PRIMARY KEY (subscription_id) USING BTREE
    table: subscriptions
    columns:
    - subscription_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (subscription_id)
    table: subscriptions
    columns:
    - subscription_id
  def: |-
    CREATE TABLE `subscriptions` (
      `subscription_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `subscription_user_id` int(8) unsigned NOT NULL,
      `subscription_type` enum('Application','Help','Registration','Test') NOT NULL,
      `subscription_type_id` int(8) unsigned DEFAULT NULL,
      `subscription_channel_email` tinyint(1) unsigned NOT NULL,
      `subscription_channel_notification` tinyint(1) unsigned NOT NULL,
      `subscription_channel_sms` tinyint(1) unsigned NOT NULL,
      `subscription_channel_whatsapp` tinyint(1) unsigned NOT NULL,
      PRIMARY KEY (`subscription_id`),
      KEY `whatsapp_user_id` (`subscription_user_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: events
  type: BASE TABLE
  columns:
  - name: event_id
    type: int(4) unsigned
    nullable: false
    extraDef: auto_increment
  - name: event_name
    type: varchar(64)
    nullable: false
  - name: event_short_name
    type: varchar(32)
    nullable: false
  - name: event_slug
    type: varchar(16)
    nullable: false
  - name: event_hidden
    type: tinyint(1)
    nullable: false
    default: "0"
  - name: event_start_time
    type: datetime
    nullable: false
  - name: event_end_time
    type: datetime
    nullable: false
  - name: hotel_enabled
    type: tinyint(1) unsigned
    nullable: false
    default: "0"
  - name: hotel_information_published
    type: tinyint(1) unsigned
    nullable: false
    default: "0"
  - name: hotel_preferences_start
    type: datetime
    nullable: true
    default: "NULL"
  - name: hotel_preferences_end
    type: datetime
    nullable: true
    default: "NULL"
  - name: refund_enabled
    type: tinyint(1) unsigned
    nullable: false
    default: "0"
  - name: refund_information_published
    type: tinyint(1) unsigned
    nullable: false
    default: "0"
  - name: refund_requests_start
    type: datetime
    nullable: true
    default: "NULL"
  - name: refund_requests_end
    type: datetime
    nullable: true
    default: "NULL"
  - name: training_enabled
    type: tinyint(1) unsigned
    nullable: false
    default: "0"
  - name: training_information_published
    type: tinyint(1) unsigned
    nullable: false
    default: "0"
  - name: training_preferences_start
    type: datetime
    nullable: true
    default: "NULL"
  - name: training_preferences_end
    type: datetime
    nullable: true
    default: "NULL"
  - name: event_availability_status
    type: enum('Unavailable','Available','Locked')
    nullable: false
  - name: event_location
    type: text
    nullable: true
    default: "NULL"
  - name: event_timezone
    type: varchar(32)
    nullable: false
  - name: event_hotel_room_form
    type: text
    nullable: true
    default: "NULL"
  - name: event_festival_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: event_identity_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: event_ytp_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  indexes:
  - name: PRIMARY
    def: PRIMARY KEY (event_id) USING BTREE
    table: events
    columns:
    - event_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (event_id)
    table: events
    columns:
    - event_id
  def: |-
    CREATE TABLE `events` (
      `event_id` int(4) unsigned NOT NULL AUTO_INCREMENT,
      `event_name` varchar(64) NOT NULL,
      `event_short_name` varchar(32) NOT NULL,
      `event_slug` varchar(16) NOT NULL,
      `event_hidden` tinyint(1) NOT NULL DEFAULT 0,
      `event_start_time` datetime NOT NULL,
      `event_end_time` datetime NOT NULL,
      `hotel_enabled` tinyint(1) unsigned NOT NULL DEFAULT 0,
      `hotel_information_published` tinyint(1) unsigned NOT NULL DEFAULT 0,
      `hotel_preferences_start` datetime DEFAULT NULL,
      `hotel_preferences_end` datetime DEFAULT NULL,
      `refund_enabled` tinyint(1) unsigned NOT NULL DEFAULT 0,
      `refund_information_published` tinyint(1) unsigned NOT NULL DEFAULT 0,
      `refund_requests_start` datetime DEFAULT NULL,
      `refund_requests_end` datetime DEFAULT NULL,
      `training_enabled` tinyint(1) unsigned NOT NULL DEFAULT 0,
      `training_information_published` tinyint(1) unsigned NOT NULL DEFAULT 0,
      `training_preferences_start` datetime DEFAULT NULL,
      `training_preferences_end` datetime DEFAULT NULL,
      `event_availability_status` enum('Unavailable','Available','Locked') NOT NULL,
      `event_location` text DEFAULT NULL,
      `event_timezone` varchar(32) NOT NULL,
      `event_hotel_room_form` text DEFAULT NULL,
      `event_festival_id` int(8) unsigned DEFAULT NULL,
      `event_identity_id` int(8) unsigned DEFAULT NULL,
      `event_ytp_id` int(8) unsigned DEFAULT NULL,
      PRIMARY KEY (`event_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: vendors
  type: BASE TABLE
  columns:
  - name: vendor_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: event_id
    type: int(8) unsigned
    nullable: false
  - name: vendor_team
    type: enum('first-aid','security')
    nullable: false
  - name: vendor_first_name
    type: varchar(64)
    nullable: false
  - name: vendor_last_name
    type: varchar(64)
    nullable: false
  - name: vendor_role
    type: varchar(32)
    nullable: false
  - name: vendor_gender
    type: enum('Female','Other','Male')
    nullable: false
  - name: vendor_shirt_size
    type: enum('XS','S','M','L','XL','XXL','3XL','4XL')
    nullable: true
    default: "NULL"
  - name: vendor_shirt_fit
    type: enum('Regular','Girly')
    nullable: true
    default: "NULL"
  - name: vendor_modified
    type: datetime
    nullable: false
  - name: vendor_visible
    type: tinyint(1)
    nullable: false
    default: "1"
  indexes:
  - name: PRIMARY
    def: PRIMARY KEY (vendor_id) USING BTREE
    table: vendors
    columns:
    - vendor_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (vendor_id)
    table: vendors
    columns:
    - vendor_id
  def: |-
    CREATE TABLE `vendors` (
      `vendor_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `event_id` int(8) unsigned NOT NULL,
      `vendor_team` enum('first-aid','security') NOT NULL,
      `vendor_first_name` varchar(64) NOT NULL,
      `vendor_last_name` varchar(64) NOT NULL,
      `vendor_role` varchar(32) NOT NULL,
      `vendor_gender` enum('Female','Other','Male') NOT NULL,
      `vendor_shirt_size` enum('XS','S','M','L','XL','XXL','3XL','4XL') DEFAULT NULL,
      `vendor_shirt_fit` enum('Regular','Girly') DEFAULT NULL,
      `vendor_modified` datetime NOT NULL,
      `vendor_visible` tinyint(1) NOT NULL DEFAULT 1,
      PRIMARY KEY (`vendor_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: tasks
  type: BASE TABLE
  columns:
  - name: task_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: task_name
    type: varchar(64)
    nullable: false
  - name: task_params
    type: text
    nullable: false
  - name: task_parent_task_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: task_scheduled_date
    type: timestamp
    nullable: false
    default: current_timestamp()
  - name: task_scheduled_interval_ms
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: task_invocation_result
    type: enum('TaskSuccess','TaskException','TaskFailure','InvalidNamedTask','InvalidParameters','UnknownFailure')
    nullable: true
    default: "NULL"
  - name: task_invocation_logs
    type: text
    nullable: true
    default: "NULL"
  - name: task_invocation_time_ms
    type: double
    nullable: true
    default: "NULL"
  indexes:
  - name: PRIMARY
    def: PRIMARY KEY (task_id) USING BTREE
    table: tasks
    columns:
    - task_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (task_id)
    table: tasks
    columns:
    - task_id
  def: |-
    CREATE TABLE `tasks` (
      `task_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `task_name` varchar(64) NOT NULL,
      `task_params` text NOT NULL,
      `task_parent_task_id` int(8) unsigned DEFAULT NULL,
      `task_scheduled_date` timestamp NOT NULL DEFAULT current_timestamp(),
      `task_scheduled_interval_ms` int(8) unsigned DEFAULT NULL,
      `task_invocation_result` enum('TaskSuccess','TaskException','TaskFailure','InvalidNamedTask','InvalidParameters','UnknownFailure') DEFAULT NULL,
      `task_invocation_logs` text DEFAULT NULL,
      `task_invocation_time_ms` double DEFAULT NULL,
      PRIMARY KEY (`task_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: shifts
  type: BASE TABLE
  columns:
  - name: shift_id
    type: int(4) unsigned
    nullable: false
    extraDef: auto_increment
  - name: event_id
    type: int(4) unsigned
    nullable: false
  - name: team_id
    type: int(4) unsigned
    nullable: false
  - name: shift_category_id
    type: int(8) unsigned
    nullable: false
  - name: shift_name
    type: varchar(64)
    nullable: false
  - name: shift_activity_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: shift_location_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: shift_colour
    type: varchar(32)
    nullable: true
    default: "NULL"
  - name: shift_description
    type: text
    nullable: true
    default: "NULL"
  - name: shift_demand
    type: text
    nullable: true
    default: "NULL"
  - name: shift_demand_overlap
    type: enum('None','Partial','Cover')
    nullable: false
    default: "'Cover'"
  - name: shift_excitement
    type: double
    nullable: false
  - name: shift_deleted
    type: datetime
    nullable: true
    default: "NULL"
  indexes:
  - name: event_id
    def: KEY event_id (event_id, team_id) USING BTREE
    table: shifts
    columns:
    - event_id
    - team_id
  - name: shifts_team_id
    def: KEY shifts_team_id (team_id) USING BTREE
    table: shifts
    columns:
    - team_id
  - name: PRIMARY
    def: PRIMARY KEY (shift_id) USING BTREE
    table: shifts
    columns:
    - shift_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (shift_id)
    table: shifts
    columns:
    - shift_id
  - name: shifts_event_id
    type: FOREIGN KEY
    def: FOREIGN KEY (event_id) REFERENCES events (event_id)
    table: shifts
    referencedTable: events
    columns:
    - event_id
    referencedColumns:
    - event_id
  - name: shifts_team_id
    type: FOREIGN KEY
    def: FOREIGN KEY (team_id) REFERENCES teams (team_id)
    table: shifts
    referencedTable: teams
    columns:
    - team_id
    referencedColumns:
    - team_id
  def: |-
    CREATE TABLE `shifts` (
      `shift_id` int(4) unsigned NOT NULL AUTO_INCREMENT,
      `event_id` int(4) unsigned NOT NULL,
      `team_id` int(4) unsigned NOT NULL,
      `shift_category_id` int(8) unsigned NOT NULL,
      `shift_name` varchar(64) NOT NULL,
      `shift_activity_id` int(8) unsigned DEFAULT NULL,
      `shift_location_id` int(8) unsigned DEFAULT NULL,
      `shift_colour` varchar(32) DEFAULT NULL,
      `shift_description` text DEFAULT NULL,
      `shift_demand` text DEFAULT NULL,
      `shift_demand_overlap` enum('None','Partial','Cover') NOT NULL DEFAULT 'Cover',
      `shift_excitement` double NOT NULL,
      `shift_deleted` datetime DEFAULT NULL,
      PRIMARY KEY (`shift_id`),
      KEY `event_id` (`event_id`,`team_id`),
      KEY `shifts_team_id` (`team_id`),
      CONSTRAINT `shifts_event_id` FOREIGN KEY (`event_id`) REFERENCES `events` (`event_id`) ON DELETE CASCADE,
      CONSTRAINT `shifts_team_id` FOREIGN KEY (`team_id`) REFERENCES `teams` (`team_id`) ON DELETE CASCADE
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: outbox_email
  type: BASE TABLE
  columns:
  - name: outbox_email_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: outbox_timestamp
    type: timestamp
    nullable: false
    default: current_timestamp()
  - name: outbox_sender
    type: varchar(128)
    nullable: false
  - name: outbox_sender_user_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: outbox_to
    type: varchar(128)
    nullable: false
  - name: outbox_to_user_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: outbox_cc
    type: varchar(128)
    nullable: true
    default: "NULL"
  - name: outbox_bcc
    type: varchar(128)
    nullable: true
    default: "NULL"
  - name: outbox_headers
    type: text
    nullable: true
    default: "NULL"
  - name: outbox_subject
    type: text
    nullable: false
  - name: outbox_body_html
    type: text
    nullable: false
    default: "''"
  - name: outbox_body_text
    type: text
    nullable: false
    default: "''"
  - name: outbox_logs
    type: text
    nullable: true
    default: "NULL"
  - name: outbox_error_name
    type: varchar(32)
    nullable: true
    default: "NULL"
  - name: outbox_error_cause
    type: text
    nullable: true
    default: "NULL"
  - name: outbox_error_message
    type: text
    nullable: true
    default: "NULL"
  - name: outbox_error_stack
    type: text
    nullable: true
    default: "NULL"
  - name: outbox_result_message_id
    type: varchar(256)
    nullable: true
    default: "NULL"
  - name: outbox_result_accepted
    type: text
    nullable: true
    default: "NULL"
  - name: outbox_result_rejected
    type: text
    nullable: true
    default: "NULL"
  - name: outbox_result_pending
    type: text
    nullable: true
    default: "NULL"
  - name: outbox_result_response
    type: text
    nullable: true
    default: "NULL"
  indexes:
  - name: outbox_date
    def: KEY outbox_date (outbox_timestamp) USING BTREE
    table: outbox_email
    columns:
    - outbox_timestamp
  - name: outbox_sender_user_id
    def: KEY outbox_sender_user_id (outbox_sender_user_id) USING BTREE
    table: outbox_email
    columns:
    - outbox_sender_user_id
  - name: outbox_to_user_id
    def: KEY outbox_to_user_id (outbox_to_user_id) USING BTREE
    table: outbox_email
    columns:
    - outbox_to_user_id
  - name: PRIMARY
    def: PRIMARY KEY (outbox_email_id) USING BTREE
    table: outbox_email
    columns:
    - outbox_email_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (outbox_email_id)
    table: outbox_email
    columns:
    - outbox_email_id
  def: |-
    CREATE TABLE `outbox_email` (
      `outbox_email_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `outbox_timestamp` timestamp NOT NULL DEFAULT current_timestamp(),
      `outbox_sender` varchar(128) NOT NULL,
      `outbox_sender_user_id` int(8) unsigned DEFAULT NULL,
      `outbox_to` varchar(128) NOT NULL,
      `outbox_to_user_id` int(8) unsigned DEFAULT NULL,
      `outbox_cc` varchar(128) DEFAULT NULL,
      `outbox_bcc` varchar(128) DEFAULT NULL,
      `outbox_headers` text DEFAULT NULL,
      `outbox_subject` text NOT NULL,
      `outbox_body_html` text NOT NULL DEFAULT '',
      `outbox_body_text` text NOT NULL DEFAULT '',
      `outbox_logs` text DEFAULT NULL,
      `outbox_error_name` varchar(32) DEFAULT NULL,
      `outbox_error_cause` text DEFAULT NULL,
      `outbox_error_message` text DEFAULT NULL,
      `outbox_error_stack` text DEFAULT NULL,
      `outbox_result_message_id` varchar(256) DEFAULT NULL,
      `outbox_result_accepted` text DEFAULT NULL,
      `outbox_result_rejected` text DEFAULT NULL,
      `outbox_result_pending` text DEFAULT NULL,
      `outbox_result_response` text DEFAULT NULL,
      PRIMARY KEY (`outbox_email_id`),
      KEY `outbox_date` (`outbox_timestamp`),
      KEY `outbox_to_user_id` (`outbox_to_user_id`),
      KEY `outbox_sender_user_id` (`outbox_sender_user_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: outbox_whatsapp
  type: BASE TABLE
  columns:
  - name: whatsapp_message_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: whatsapp_date
    type: datetime
    nullable: false
    default: current_timestamp()
  - name: whatsapp_recipient_user_id
    type: int(8) unsigned
    nullable: false
  - name: whatsapp_recipient_phone_number
    type: varchar(16)
    nullable: false
  - name: whatsapp_request
    type: text
    nullable: false
  - name: whatsapp_error_name
    type: varchar(32)
    nullable: true
    default: "NULL"
  - name: whatsapp_error_message
    type: text
    nullable: true
    default: "NULL"
  - name: whatsapp_error_stack
    type: text
    nullable: true
    default: "NULL"
  - name: whatsapp_error_cause
    type: text
    nullable: true
    default: "NULL"
  - name: whatsapp_response_status
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: whatsapp_response_time
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: whatsapp_response_message_id
    type: text
    nullable: true
    default: "NULL"
  - name: whatsapp_response_message_status
    type: text
    nullable: true
    default: "NULL"
  - name: whatsapp_response_text
    type: text
    nullable: true
    default: "NULL"
  indexes:
  - name: whatsapp_message_recipient_user_id
    def: KEY whatsapp_message_recipient_user_id (whatsapp_recipient_user_id) USING BTREE
    table: outbox_whatsapp
    columns:
    - whatsapp_recipient_user_id
  - name: PRIMARY
    def: PRIMARY KEY (whatsapp_message_id) USING BTREE
    table: outbox_whatsapp
    columns:
    - whatsapp_message_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (whatsapp_message_id)
    table: outbox_whatsapp
    columns:
    - whatsapp_message_id
  def: |-
    CREATE TABLE `outbox_whatsapp` (
      `whatsapp_message_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `whatsapp_date` datetime NOT NULL DEFAULT current_timestamp(),
      `whatsapp_recipient_user_id` int(8) unsigned NOT NULL,
      `whatsapp_recipient_phone_number` varchar(16) NOT NULL,
      `whatsapp_request` text NOT NULL,
      `whatsapp_error_name` varchar(32) DEFAULT NULL,
      `whatsapp_error_message` text DEFAULT NULL,
      `whatsapp_error_stack` text DEFAULT NULL,
      `whatsapp_error_cause` text DEFAULT NULL,
      `whatsapp_response_status` int(8) unsigned DEFAULT NULL,
      `whatsapp_response_time` int(8) unsigned DEFAULT NULL,
      `whatsapp_response_message_id` text DEFAULT NULL,
      `whatsapp_response_message_status` text DEFAULT NULL,
      `whatsapp_response_text` text DEFAULT NULL,
      PRIMARY KEY (`whatsapp_message_id`),
      KEY `whatsapp_message_recipient_user_id` (`whatsapp_recipient_user_id`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: users_events_favourites
  type: BASE TABLE
  columns:
  - name: user_id
    type: int(4) unsigned
    nullable: false
  - name: event_id
    type: int(4) unsigned
    nullable: false
  - name: activity_id
    type: int(4) unsigned
    nullable: false
  indexes:
  - name: user_id
    def: KEY user_id (user_id, event_id) USING BTREE
    table: users_events_favourites
    columns:
    - user_id
    - event_id
  - name: user_id_2
    def: UNIQUE KEY user_id_2 (user_id, event_id, activity_id) USING BTREE
    table: users_events_favourites
    columns:
    - user_id
    - event_id
    - activity_id
  constraints:
  - name: user_id_2
    type: UNIQUE
    def: UNIQUE KEY user_id_2 (user_id, event_id, activity_id)
    table: users_events_favourites
    columns:
    - user_id
    - event_id
    - activity_id
  def: |-
    CREATE TABLE `users_events_favourites` (
      `user_id` int(4) unsigned NOT NULL,
      `event_id` int(4) unsigned NOT NULL,
      `activity_id` int(4) unsigned NOT NULL,
      UNIQUE KEY `user_id_2` (`user_id`,`event_id`,`activity_id`),
      KEY `user_id` (`user_id`,`event_id`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: logs
  type: BASE TABLE
  columns:
  - name: log_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: log_date
    type: timestamp
    nullable: false
    default: current_timestamp()
  - name: log_type
    type: varchar(48)
    nullable: false
  - name: log_severity
    type: enum('Debug','Info','Warning','Error')
    nullable: false
  - name: log_source_user_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: log_source_ip_address
    type: varchar(45)
    nullable: true
    default: "NULL"
  - name: log_source_user_agent
    type: text
    nullable: true
    default: "NULL"
  - name: log_target_user_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: log_data
    type: text
    nullable: true
    default: "NULL"
  - name: log_deleted
    type: datetime
    nullable: true
    default: "NULL"
  indexes:
  - name: log_source_user_id
    def: KEY log_source_user_id (log_source_user_id) USING BTREE
    table: logs
    columns:
    - log_source_user_id
  - name: log_target_user_id
    def: KEY log_target_user_id (log_target_user_id) USING BTREE
    table: logs
    columns:
    - log_target_user_id
  - name: PRIMARY
    def: PRIMARY KEY (log_id) USING BTREE
    table: logs
    columns:
    - log_id
  constraints:
  - name: logs_log_source_user_id
    type: FOREIGN KEY
    def: FOREIGN KEY (log_source_user_id) REFERENCES users (user_id)
    table: logs
    referencedTable: users
    columns:
    - log_source_user_id
    referencedColumns:
    - user_id
  - name: logs_log_target_user_id
    type: FOREIGN KEY
    def: FOREIGN KEY (log_target_user_id) REFERENCES users (user_id)
    table: logs
    referencedTable: users
    columns:
    - log_target_user_id
    referencedColumns:
    - user_id
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (log_id)
    table: logs
    columns:
    - log_id
  def: |-
    CREATE TABLE `logs` (
      `log_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `log_date` timestamp NOT NULL DEFAULT current_timestamp(),
      `log_type` varchar(48) NOT NULL,
      `log_severity` enum('Debug','Info','Warning','Error') NOT NULL,
      `log_source_user_id` int(8) unsigned DEFAULT NULL,
      `log_source_ip_address` varchar(45) DEFAULT NULL,
      `log_source_user_agent` text DEFAULT NULL,
      `log_target_user_id` int(8) unsigned DEFAULT NULL,
      `log_data` text DEFAULT NULL,
      `log_deleted` datetime DEFAULT NULL,
      PRIMARY KEY (`log_id`),
      KEY `log_target_user_id` (`log_target_user_id`),
      KEY `log_source_user_id` (`log_source_user_id`),
      CONSTRAINT `logs_log_source_user_id` FOREIGN KEY (`log_source_user_id`) REFERENCES `users` (`user_id`),
      CONSTRAINT `logs_log_target_user_id` FOREIGN KEY (`log_target_user_id`) REFERENCES `users` (`user_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: schedule
  type: BASE TABLE
  columns:
  - name: schedule_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: user_id
    type: int(8) unsigned
    nullable: false
  - name: event_id
    type: int(8) unsigned
    nullable: false
  - name: shift_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: schedule_time_start
    type: datetime
    nullable: false
  - name: schedule_time_end
    type: datetime
    nullable: false
  - name: schedule_updated_by
    type: int(8) unsigned
    nullable: false
  - name: schedule_updated
    type: datetime
    nullable: false
  - name: schedule_deleted
    type: datetime
    nullable: true
    default: "NULL"
  indexes:
  - name: schedule_event_id
    def: KEY schedule_event_id (event_id) USING BTREE
    table: schedule
    columns:
    - event_id
  - name: schedule_shift_id
    def: KEY schedule_shift_id (shift_id) USING BTREE
    table: schedule
    columns:
    - shift_id
  - name: schedule_updated_by
    def: KEY schedule_updated_by (schedule_updated_by) USING BTREE
    table: schedule
    columns:
    - schedule_updated_by
  - name: schedule_user_id
    def: KEY schedule_user_id (user_id) USING BTREE
    table: schedule
    columns:
    - user_id
  - name: PRIMARY
    def: PRIMARY KEY (schedule_id) USING BTREE
    table: schedule
    columns:
    - schedule_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (schedule_id)
    table: schedule
    columns:
    - schedule_id
  - name: schedule_event_id
    type: FOREIGN KEY
    def: FOREIGN KEY (event_id) REFERENCES events (event_id)
    table: schedule
    referencedTable: events
    columns:
    - event_id
    referencedColumns:
    - event_id
  - name: schedule_shift_id
    type: FOREIGN KEY
    def: FOREIGN KEY (shift_id) REFERENCES shifts (shift_id)
    table: schedule
    referencedTable: shifts
    columns:
    - shift_id
    referencedColumns:
    - shift_id
  - name: schedule_updated_by
    type: FOREIGN KEY
    def: FOREIGN KEY (schedule_updated_by) REFERENCES users (user_id)
    table: schedule
    referencedTable: users
    columns:
    - schedule_updated_by
    referencedColumns:
    - user_id
  - name: schedule_user_id
    type: FOREIGN KEY
    def: FOREIGN KEY (user_id) REFERENCES users (user_id)
    table: schedule
    referencedTable: users
    columns:
    - user_id
    referencedColumns:
    - user_id
  def: |-
    CREATE TABLE `schedule` (
      `schedule_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `user_id` int(8) unsigned NOT NULL,
      `event_id` int(8) unsigned NOT NULL,
      `shift_id` int(8) unsigned DEFAULT NULL,
      `schedule_time_start` datetime NOT NULL,
      `schedule_time_end` datetime NOT NULL,
      `schedule_updated_by` int(8) unsigned NOT NULL,
      `schedule_updated` datetime NOT NULL,
      `schedule_deleted` datetime DEFAULT NULL,
      PRIMARY KEY (`schedule_id`),
      KEY `schedule_event_id` (`event_id`),
      KEY `schedule_shift_id` (`shift_id`),
      KEY `schedule_user_id` (`user_id`),
      KEY `schedule_updated_by` (`schedule_updated_by`),
      CONSTRAINT `schedule_event_id` FOREIGN KEY (`event_id`) REFERENCES `events` (`event_id`) ON DELETE CASCADE,
      CONSTRAINT `schedule_shift_id` FOREIGN KEY (`shift_id`) REFERENCES `shifts` (`shift_id`) ON DELETE CASCADE,
      CONSTRAINT `schedule_updated_by` FOREIGN KEY (`schedule_updated_by`) REFERENCES `users` (`user_id`) ON DELETE CASCADE,
      CONSTRAINT `schedule_user_id` FOREIGN KEY (`user_id`) REFERENCES `users` (`user_id`) ON DELETE CASCADE
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: content_category
  type: BASE TABLE
  columns:
  - name: category_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: event_id
    type: int(8) unsigned
    nullable: false
  - name: category_title
    type: text
    nullable: false
  - name: category_icon
    type: text
    nullable: false
  - name: category_description
    type: text
    nullable: true
    default: "NULL"
  - name: category_permission
    type: text
    nullable: true
    default: "NULL"
  - name: category_order
    type: int(8) unsigned
    nullable: false
  - name: category_created
    type: datetime
    nullable: false
  - name: category_updated
    type: datetime
    nullable: false
  - name: category_deleted
    type: datetime
    nullable: true
    default: "NULL"
  indexes:
  - name: event_id
    def: KEY event_id (event_id) USING BTREE
    table: content_category
    columns:
    - event_id
  - name: PRIMARY
    def: PRIMARY KEY (category_id) USING BTREE
    table: content_category
    columns:
    - category_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (category_id)
    table: content_category
    columns:
    - category_id
  def: |-
    CREATE TABLE `content_category` (
      `category_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `event_id` int(8) unsigned NOT NULL,
      `category_title` text NOT NULL,
      `category_icon` text NOT NULL,
      `category_description` text DEFAULT NULL,
      `category_permission` text DEFAULT NULL,
      `category_order` int(8) unsigned NOT NULL,
      `category_created` datetime NOT NULL,
      `category_updated` datetime NOT NULL,
      `category_deleted` datetime DEFAULT NULL,
      PRIMARY KEY (`category_id`),
      KEY `event_id` (`event_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: events_sales_bak
  type: BASE TABLE
  columns:
  - name: event_id
    type: int(8) unsigned
    nullable: false
  - name: event_sale_id
    type: int(8) unsigned
    nullable: false
  - name: event_sale_date
    type: date
    nullable: false
  - name: event_sale_count
    type: int(8) unsigned
    nullable: false
  - name: event_sale_updated
    type: datetime
    nullable: false
  indexes:
  - name: event_id
    def: UNIQUE KEY event_id (event_id, event_sale_id, event_sale_date) USING BTREE
    table: events_sales_bak
    columns:
    - event_id
    - event_sale_id
    - event_sale_date
  constraints:
  - name: event_id
    type: UNIQUE
    def: UNIQUE KEY event_id (event_id, event_sale_id, event_sale_date)
    table: events_sales_bak
    columns:
    - event_id
    - event_sale_id
    - event_sale_date
  def: |-
    CREATE TABLE `events_sales_bak` (
      `event_id` int(8) unsigned NOT NULL,
      `event_sale_id` int(8) unsigned NOT NULL,
      `event_sale_date` date NOT NULL,
      `event_sale_count` int(8) unsigned NOT NULL,
      `event_sale_updated` datetime NOT NULL,
      UNIQUE KEY `event_id` (`event_id`,`event_sale_id`,`event_sale_date`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: content
  type: BASE TABLE
  columns:
  - name: content_id
    type: int(10) unsigned
    nullable: false
    extraDef: auto_increment
  - name: event_id
    type: int(4) unsigned
    nullable: false
  - name: team_id
    type: int(4) unsigned
    nullable: false
  - name: content_path
    type: text
    nullable: false
  - name: content_title
    type: text
    nullable: false
  - name: content_type
    type: enum('Page','FAQ')
    nullable: false
  - name: content_category_id
    type: int(8) unsigned
    nullable: true
    default: "NULL"
  - name: content_protected
    type: tinyint(1)
    nullable: false
    default: "0"
  - name: content
    type: text
    nullable: false
  - name: revision_author_id
    type: int(4) unsigned
    nullable: false
  - name: revision_date
    type: timestamp
    nullable: false
    default: current_timestamp()
  - name: revision_visible
    type: tinyint(1)
    nullable: false
    default: "1"
  indexes:
  - name: event_id
    def: KEY event_id (event_id, team_id) USING BTREE
    table: content
    columns:
    - event_id
    - team_id
  - name: PRIMARY
    def: PRIMARY KEY (content_id) USING BTREE
    table: content
    columns:
    - content_id
  - name: event_id_2
    def: UNIQUE KEY event_id_2 (event_id, team_id, content_path, revision_visible) USING HASH
    table: content
    columns:
    - event_id
    - team_id
    - content_path
    - revision_visible
  constraints:
  - name: event_id_2
    type: UNIQUE
    def: UNIQUE KEY event_id_2 (event_id, team_id, content_path, revision_visible)
    table: content
    columns:
    - event_id
    - team_id
    - content_path
    - revision_visible
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (content_id)
    table: content
    columns:
    - content_id
  def: |-
    CREATE TABLE `content` (
      `content_id` int(10) unsigned NOT NULL AUTO_INCREMENT,
      `event_id` int(4) unsigned NOT NULL,
      `team_id` int(4) unsigned NOT NULL,
      `content_path` text NOT NULL,
      `content_title` text NOT NULL,
      `content_type` enum('Page','FAQ') NOT NULL,
      `content_category_id` int(8) unsigned DEFAULT NULL,
      `content_protected` tinyint(1) NOT NULL DEFAULT 0,
      `content` text NOT NULL,
      `revision_author_id` int(4) unsigned NOT NULL,
      `revision_date` timestamp NOT NULL DEFAULT current_timestamp(),
      `revision_visible` tinyint(1) NOT NULL DEFAULT 1,
      PRIMARY KEY (`content_id`),
      UNIQUE KEY `event_id_2` (`event_id`,`team_id`,`content_path`,`revision_visible`) USING HASH,
      KEY `event_id` (`event_id`,`team_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: vendors_schedule
  type: BASE TABLE
  columns:
  - name: vendors_schedule_id
    type: int(8)
    nullable: false
    extraDef: auto_increment
  - name: vendor_id
    type: int(8)
    nullable: false
  - name: vendors_schedule_start
    type: datetime
    nullable: false
  - name: vendors_schedule_end
    type: datetime
    nullable: false
  - name: vendors_schedule_created
    type: datetime
    nullable: false
  - name: vendors_schedule_updated
    type: datetime
    nullable: false
  - name: vendors_schedule_deleted
    type: datetime
    nullable: true
    default: "NULL"
  indexes:
  - name: vendor_id
    def: KEY vendor_id (vendor_id) USING BTREE
    table: vendors_schedule
    columns:
    - vendor_id
  - name: PRIMARY
    def: PRIMARY KEY (vendors_schedule_id) USING BTREE
    table: vendors_schedule
    columns:
    - vendors_schedule_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (vendors_schedule_id)
    table: vendors_schedule
    columns:
    - vendors_schedule_id
  def: |-
    CREATE TABLE `vendors_schedule` (
      `vendors_schedule_id` int(8) NOT NULL AUTO_INCREMENT,
      `vendor_id` int(8) NOT NULL,
      `vendors_schedule_start` datetime NOT NULL,
      `vendors_schedule_end` datetime NOT NULL,
      `vendors_schedule_created` datetime NOT NULL,
      `vendors_schedule_updated` datetime NOT NULL,
      `vendors_schedule_deleted` datetime DEFAULT NULL,
      PRIMARY KEY (`vendors_schedule_id`),
      KEY `vendor_id` (`vendor_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: events_sales_configuration
  type: BASE TABLE
  columns:
  - name: event_id
    type: int(11)
    nullable: false
  - name: sale_id
    type: int(8) unsigned
    nullable: false
  - name: sale_category
    type: enum('Event','Hidden','Locker','TicketFriday','TicketSaturday','TicketSunday','TicketWeekend')
    nullable: true
    default: "NULL"
  - name: sale_category_limit
    type: int(4) unsigned
    nullable: true
    default: "NULL"
  - name: sale_description
    type: text
    nullable: true
    default: "NULL"
  - name: sale_price
    type: double unsigned
    nullable: true
    default: "NULL"
  - name: sale_product
    type: text
    nullable: false
  - name: sale_event_id
    type: int(4) unsigned
    nullable: true
    default: "NULL"
  indexes:
  - name: event_id_2
    def: KEY event_id_2 (event_id) USING BTREE
    table: events_sales_configuration
    columns:
    - event_id
  - name: event_id
    def: UNIQUE KEY event_id (event_id, sale_id) USING BTREE
    table: events_sales_configuration
    columns:
    - event_id
    - sale_id
  constraints:
  - name: event_id
    type: UNIQUE
    def: UNIQUE KEY event_id (event_id, sale_id)
    table: events_sales_configuration
    columns:
    - event_id
    - sale_id
  def: |-
    CREATE TABLE `events_sales_configuration` (
      `event_id` int(11) NOT NULL,
      `sale_id` int(8) unsigned NOT NULL,
      `sale_category` enum('Event','Hidden','Locker','TicketFriday','TicketSaturday','TicketSunday','TicketWeekend') DEFAULT NULL,
      `sale_category_limit` int(4) unsigned DEFAULT NULL,
      `sale_description` text DEFAULT NULL,
      `sale_price` double unsigned DEFAULT NULL,
      `sale_product` text NOT NULL,
      `sale_event_id` int(4) unsigned DEFAULT NULL,
      UNIQUE KEY `event_id` (`event_id`,`sale_id`),
      KEY `event_id_2` (`event_id`)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
- name: nardo_personalised
  type: BASE TABLE
  columns:
  - name: nardo_personalised_id
    type: int(8) unsigned
    nullable: false
    extraDef: auto_increment
  - name: nardo_personalised_user_id
    type: int(8) unsigned
    nullable: false
  - name: nardo_personalised_date
    type: datetime
    nullable: false
  - name: nardo_personalised_input
    type: text
    nullable: false
  - name: nardo_personalised_output
    type: text
    nullable: false
  indexes:
  - name: PRIMARY
    def: PRIMARY KEY (nardo_personalised_id) USING BTREE
    table: nardo_personalised
    columns:
    - nardo_personalised_id
  constraints:
  - name: PRIMARY
    type: PRIMARY KEY
    def: PRIMARY KEY (nardo_personalised_id)
    table: nardo_personalised
    columns:
    - nardo_personalised_id
  def: |-
    CREATE TABLE `nardo_personalised` (
      `nardo_personalised_id` int(8) unsigned NOT NULL AUTO_INCREMENT,
      `nardo_personalised_user_id` int(8) unsigned NOT NULL,
      `nardo_personalised_date` datetime NOT NULL,
      `nardo_personalised_input` text NOT NULL,
      `nardo_personalised_output` text NOT NULL,
      PRIMARY KEY (`nardo_personalised_id`)
    ) ENGINE=InnoDB AUTO_INCREMENT=[Redacted by tbls] DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
relations:
- table: events_teams
  columns:
  - event_id
  cardinality: zero_or_more
  parentTable: events
  parentColumns:
  - event_id
  parentCardinality: exactly_one
  def: FOREIGN KEY (event_id) REFERENCES events (event_id)
  virtual: false
- table: events_teams
  columns:
  - team_id
  cardinality: zero_or_more
  parentTable: teams
  parentColumns:
  - team_id
  parentCardinality: exactly_one
  def: FOREIGN KEY (team_id) REFERENCES teams (team_id)
  virtual: false
- table: hotels
  columns:
  - event_id
  cardinality: zero_or_more
  parentTable: events
  parentColumns:
  - event_id
  parentCardinality: exactly_one
  def: FOREIGN KEY (event_id) REFERENCES events (event_id)
  virtual: false
- table: storage
  columns:
  - user_id
  cardinality: zero_or_more
  parentTable: users
  parentColumns:
  - user_id
  parentCardinality: zero_or_one
  def: FOREIGN KEY (user_id) REFERENCES users (user_id)
  virtual: false
- table: teams_roles
  columns:
  - role_id
  cardinality: zero_or_more
  parentTable: roles
  parentColumns:
  - role_id
  parentCardinality: exactly_one
  def: FOREIGN KEY (role_id) REFERENCES roles (role_id)
  virtual: false
- table: teams_roles
  columns:
  - team_id
  cardinality: zero_or_more
  parentTable: teams
  parentColumns:
  - team_id
  parentCardinality: exactly_one
  def: FOREIGN KEY (team_id) REFERENCES teams (team_id)
  virtual: false
- table: trainings
  columns:
  - event_id
  cardinality: zero_or_more
  parentTable: events
  parentColumns:
  - event_id
  parentCardinality: exactly_one
  def: FOREIGN KEY (event_id) REFERENCES events (event_id)
  virtual: false
- table: users_auth
  columns:
  - user_id
  cardinality: zero_or_more
  parentTable: users
  parentColumns:
  - user_id
  parentCardinality: exactly_one
  def: FOREIGN KEY (user_id) REFERENCES users (user_id)
  virtual: false
- table: users_events
  columns:
  - event_id
  cardinality: zero_or_more
  parentTable: events
  parentColumns:
  - event_id
  parentCardinality: exactly_one
  def: FOREIGN KEY (event_id) REFERENCES events (event_id)
  virtual: false
- table: users_events
  columns:
  - role_id
  cardinality: zero_or_more
  parentTable: roles
  parentColumns:
  - role_id
  parentCardinality: exactly_one
  def: FOREIGN KEY (role_id) REFERENCES roles (role_id)
  virtual: false
- table: users_events
  columns:
  - team_id
  cardinality: zero_or_more
  parentTable: teams
  parentColumns:
  - team_id
  parentCardinality: exactly_one
  def: FOREIGN KEY (team_id) REFERENCES teams (team_id)
  virtual: false
- table: users_events
  columns:
  - user_id
  cardinality: zero_or_more
  parentTable: users
  parentColumns:
  - user_id
  parentCardinality: exactly_one
  def: FOREIGN KEY (user_id) REFERENCES users (user_id)
  virtual: false
- table: users_settings
  columns:
  - user_id
  cardinality: zero_or_more
  parentTable: users
  parentColumns:
  - user_id
  parentCardinality: exactly_one
  def: FOREIGN KEY (user_id) REFERENCES users (user_id)
  virtual: false
- table: shifts
  columns:
  - event_id
  cardinality: zero_or_more
  parentTable: events
  parentColumns:
  - event_id
  parentCardinality: exactly_one
  def: FOREIGN KEY (event_id) REFERENCES events (event_id)
  virtual: false
- table: shifts
  columns:
  - team_id
  cardinality: zero_or_more
  parentTable: teams
  parentColumns:
  - team_id
  parentCardinality: exactly_one
  def: FOREIGN KEY (team_id) REFERENCES teams (team_id)
  virtual: false
- table: logs
  columns:
  - log_source_user_id
  cardinality: zero_or_more
  parentTable: users
  parentColumns:
  - user_id
  parentCardinality: zero_or_one
  def: FOREIGN KEY (log_source_user_id) REFERENCES users (user_id)
  virtual: false
- table: logs
  columns:
  - log_target_user_id
  cardinality: zero_or_more
  parentTable: users
  parentColumns:
  - user_id
  parentCardinality: zero_or_one
  def: FOREIGN KEY (log_target_user_id) REFERENCES users (user_id)
  virtual: false
- table: schedule
  columns:
  - event_id
  cardinality: zero_or_more
  parentTable: events
  parentColumns:
  - event_id
  parentCardinality: exactly_one
  def: FOREIGN KEY (event_id) REFERENCES events (event_id)
  virtual: false
- table: schedule
  columns:
  - shift_id
  cardinality: zero_or_more
  parentTable: shifts
  parentColumns:
  - shift_id
  parentCardinality: zero_or_one
  def: FOREIGN KEY (shift_id) REFERENCES shifts (shift_id)
  virtual: false
- table: schedule
  columns:
  - schedule_updated_by
  cardinality: zero_or_more
  parentTable: users
  parentColumns:
  - user_id
  parentCardinality: exactly_one
  def: FOREIGN KEY (schedule_updated_by) REFERENCES users (user_id)
  virtual: false
- table: schedule
  columns:
  - user_id
  cardinality: zero_or_more
  parentTable: users
  parentColumns:
  - user_id
  parentCardinality: exactly_one
  def: FOREIGN KEY (user_id) REFERENCES users (user_id)
  virtual: false
driver:
  name: mysql
  databaseVersion: 10.6.14-MariaDB
  meta:
    dict: {}
